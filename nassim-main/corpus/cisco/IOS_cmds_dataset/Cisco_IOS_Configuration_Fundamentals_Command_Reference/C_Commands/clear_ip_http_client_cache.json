{
    "From": " Cisco IOS Configuration Fundamentals Command Reference",
    "PageTitle": "clear ip http client cache",
    "FuncDef": [
        "To remove information from the HTTP client cache, use the **clear ip http client cache** command in privileged EXEC mode. "
    ],
    "CLIs": [
        "clear ip http client cache { all | session <session-name> | url <complete-url> }"
    ],
    "ParentView": [
        null
    ],
    "ParaDef": [
        {
            "Parameters": " **cache all** ",
            "Info": " Removes all HTTP client cache entries. "
        },
        {
            "Parameters": " **cache session** *session-name* ",
            "Info": " Removes HTTP client cache entries of the HTTP client application session specified by the *session-name* argument. "
        },
        {
            "Parameters": " **cache url** *complete-url* ",
            "Info": " Removes the HTTP client cache entry whose location is specified by the *complete-url* argument, a Cisco IOS File System (IFS) Uniform Resource Locator (URL), and that consists of HTML files used by an HTTP server. "
        }
    ],
    "Examples": [
        [
            "The following example clears all entries in the HTTP client cache:",
            "```",
            "Router# clear ip http client cache all",
            "```",
            "```",
            "```",
            "The following example removes HTTP client cache entries that belong to the HTTP Client File System (CFS) application:",
            "```",
            "Router# clear ip http client cache session HTTP CFS",
            "```",
            "```",
            "```",
            "The following example removes HTTP client cache entries at the location http://myrouter.cisco.com/flash:/:",
            "```",
            "Router# clear ip http client cache url http://myrouter.cisco.com/flash:/",
            "```"
        ]
    ],
    "ExtraInfo": "Use this command to clear entries from the HTTP client cache pool: all the entries, all the entries owned by a specific session, or only the entry associated with a specific request from an HTTP server.",
    "RelatedCmds": [
        {
            "cmd": " **ip http path** ",
            "description": " Specifies the base path used to locate files for use by the HTTP server. "
        },
        {
            "cmd": " **show ip http client** ",
            "description": " Displays a report about the HTTP client. "
        }
    ]
}