{
    "From": " Cisco IOS Configuration Fundamentals Command Reference",
    "PageTitle": "scheduler interrupt mask time",
    "FuncDef": [
        "To configure the maximum time that a process can run with interrupts masked before another entry is created in the interrupt mask buffer, use the **scheduler interrupt mask time** command in global configuration mode. To reset the threshold time to the default, use the **no** form of this command. "
    ],
    "CLIs": [
        "scheduler interrupt mask time <threshold-time>",
        "no scheduler interrupt mask time"
    ],
    "ParentView": [
        null
    ],
    "ParaDef": [
        {
            "Parameters": " *threshold-time* ",
            "Info": " Specifies the maximum amount of timein microseconds a process can be in interrupt masked state without creating an entry in the interrupt mask buffer. "
        }
    ],
    "Examples": [
        [
            "The following shows how to configure 100 microseconds as the maximum time a process can run with interrupts masked before another entry is created in the interrupt mask buffer:",
            "```",
            "Router(config)# scheduler interrupt mask time 100",
            "```"
        ]
    ],
    "ExtraInfo": null,
    "RelatedCmds": [
        {
            "cmd": " **clear processes interrupt mask detail** ",
            "description": " Clears the interrupt masked details for all processes and stack traces that have been dumped into the interrupt mask buffer. "
        },
        {
            "cmd": " **scheduler interrupt mask profile** ",
            "description": " Enables or disables interrupt mask profiling for all processes running on the system. "
        },
        {
            "cmd": " **scheduler interrupt mask size** ",
            "description": " Configures the maximum number of entries that can exist in the interrupt mask buffer. "
        },
        {
            "cmd": " show processes interrupt mask buffer ",
            "description": " Displays the information stored in the interrupt mask buffer. "
        },
        {
            "cmd": " **show processes interrupt mask detail** ",
            "description": " Displays interrupt masked details for the specified process or all processes in the system. "
        }
    ]
}