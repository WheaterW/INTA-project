{
    "From": " Cisco IOS Configuration Fundamentals Command Reference",
    "PageTitle": "default-value exec-character-bits",
    "FuncDef": [
        "To define the EXEC character width for either 7 bits or 8 bits, use the **default-value exec-character-bits** command in global configuration mode. To restore the default value, use the **no** form of this command. "
    ],
    "CLIs": [
        "default-value exec-character-bits { 7 | 8 }",
        "no default-value exec-character-bits"
    ],
    "ParentView": [
        null
    ],
    "ParaDef": [
        {
            "Parameters": " **7** ",
            "Info": " Selects the 7-bit ASCII character set. This is the default. "
        },
        {
            "Parameters": " **8** ",
            "Info": " Selects the full 8-bit ASCII character set. "
        }
    ],
    "Examples": [
        [
            "The following example selects the full 8-bit ASCII character set for EXEC banners and prompts:",
            "```",
            "Router(config)# default-value exec-character-bits 8",
            "```"
        ]
    ],
    "ExtraInfo": "Configuring the EXEC character width to 8 bits allows you to add graphical and international characters in banners, prompts, and so on. However, setting the EXEC character width to 8 bits can also cause failures. If a user on a terminal that is sending parity enters the **help** command, an \"unrecognized command\" message appears because the system is reading all 8 bits, although the eighth bit is not needed for the **help** command.",
    "RelatedCmds": [
        {
            "cmd": " **default-value special-character-bits** ",
            "description": " Configures the flow control default value from a 7-bit width to an 8-bit width. "
        },
        {
            "cmd": " **exec-character-bits** ",
            "description": " Configures the character widths of EXEC and configuration command characters. "
        },
        {
            "cmd": " **length** ",
            "description": " Sets the terminal screen length. "
        },
        {
            "cmd": " **terminal exec-character-bits** ",
            "description": " Locally changes the ASCII character set used in EXEC and configuration command characters for the current session. "
        },
        {
            "cmd": " **terminal special-character-bits** ",
            "description": " Changes the ASCII character widths to accept special characters for the current terminal line and session. "
        }
    ]
}