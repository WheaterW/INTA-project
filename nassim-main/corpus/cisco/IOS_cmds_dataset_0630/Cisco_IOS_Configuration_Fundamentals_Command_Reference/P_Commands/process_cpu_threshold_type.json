{
    "From": " Cisco IOS Configuration Fundamentals Command Reference",
    "PageTitle": "process cpu threshold type",
    "FuncDef": [
        "To set CPU thresholding notification types and values, use the **process cpu threshold type** command in global configuration mode. To disable CPU thresholding notifications, use the **no** form of this command. "
    ],
    "CLIs": [
        "process cpu threshold type { total | process | interrupt } rising <percentage> interval <seconds> [ falling <fall-percentage> interval <seconds> ]",
        "no process cpu threshold type { total | process | interrupt }"
    ],
    "ParentView": [
        null
    ],
    "ParaDef": [
        {
            "Parameters": " **total** ",
            "Info": " Sets the CPU threshold type to total CPU utilization. "
        },
        {
            "Parameters": " **process** ",
            "Info": " Sets the CPU threshold type to CPU process utilization. "
        },
        {
            "Parameters": " **interrupt** ",
            "Info": " Sets the CPU threshold type to CPU interrupt utilization. "
        },
        {
            "Parameters": " **rising** *percentage* ",
            "Info": " The percentage (1 to 100) of CPU resources that, when exceeded for the configured interval, triggers a CPU thresholding notification. "
        },
        {
            "Parameters": " **interval** *seconds* ",
            "Info": " The duration of the CPU threshold violation, in seconds (5 to 86400), that must be met to trigger a CPU thresholding notification. "
        },
        {
            "Parameters": " **falling** *fall-percentage* ",
            "Info": " (Optional) The percentage (1 to 100) of CPU resources that, when usage falls below this level for the configured interval, triggers a CPU thresholding notification.  •This value must be equal to or less than the **rising** *percentage* value.  •If not specified, the **falling** *fall-percentage* value is set to the same value as the **rising** *percentage* value. "
        }
    ],
    "Examples": [
        [
            "The following example shows how to set the total CPU utilization notification threshold at 80 percent for a rising threshold notification and 20 percent for a falling threshold notification, with a 5-second polling interval:",
            "```",
            "configure terminal",
            "```",
            "```",
            "!",
            "```",
            "```",
            "process cpu threshold type total rising 80 interval 5 falling 20 interval 5",
            "```",
            "```",
            "end",
            "```"
        ]
    ],
    "ExtraInfo": "This command defines CPU usage thresholds that, when crossed, cause a CPU thresholding notification. When this command is enabled, Cisco IOS software polls the system at the configured interval. Notification occurs in two situations:•![](/c/dam/en/us/td/i/templates/blank.gif)When a configured CPU usage threshold is exceeded (**rising** *percentage*)•![](/c/dam/en/us/td/i/templates/blank.gif)When CPU usage falls below the configured threshold (**falling** *fall-percentage*)",
    "RelatedCmds": [
        {
            "cmd": " **process cpu statistics limit entry** ",
            "description": " Sets the entry limit and size of CPU utilization statistics. "
        },
        {
            "cmd": " **snmp-server enable traps cpu** ",
            "description": " Enables CPU threshold violations traps. "
        },
        {
            "cmd": " **snmp-server host** ",
            "description": " Specifies the recipient of SNMP notifications. "
        }
    ]
}