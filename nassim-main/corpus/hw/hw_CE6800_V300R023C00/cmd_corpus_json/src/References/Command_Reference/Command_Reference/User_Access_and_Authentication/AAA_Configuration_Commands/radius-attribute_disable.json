{
    "PageTitle": "radius-attribute disable",
    "FuncDef": "The radius-attribute disable command disables a RADIUS attribute. The undo radius-attribute disable command enables a disabled RADIUS attribute. By default, no RADIUS attribute is disabled.",
    "CLIs": [
        "radius-attribute disable <attribute-name> { receive | send }",
        "undo radius-attribute disable [ <attribute-name> ]"
    ],
    "ParentView": [
        "RADIUS server template view"
    ],
    "ParaDef": [
        {
            "Parameters": "<attribute-name>",
            "Info": "Specifies the name of a RADIUS attribute.\nThe value is a string of 1 to 64 characters. After the name is entered, the system automatically associates the RADIUS attribute with the name."
        },
        {
            "Parameters": "receive",
            "Info": "Disables a RADIUS attribute for received packets.\n-"
        },
        {
            "Parameters": "send",
            "Info": "Disables a RADIUS attribute for sent packets.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] radius-server template test1",
            "[*HUAWEI-radius-test1] radius-server attribute translate",
            "[*HUAWEI-radius-test1] radius-attribute disable NAS-Port-Type send"
        ]
    ],
    "ExtraInfo": "Generally, a RADIUS server connects to multiple network devices, which can be one vendor's devices or different vendors' devices. If some vendors' devices require the RADIUS server to deliver an attribute to support a specified feature but other vendors' device do not support the delivered attribute, the RADIUS attribute may fail to be parsed."
}