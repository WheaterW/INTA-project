{
    "PageTitle": "ip route-static default-preference",
    "FuncDef": "The ip route-static default-preference command sets the default priority for IPv4 static routes.\nThe undo ip route-static default-preference command restores the default setting. The ip route-static default-preference command sets the default priority for IPv4 static routes. The undo ip route-static default-preference command restores the default setting. By default, the default priority of static routes is 60.",
    "CLIs": [
        "ip route-static default-preference <preference>",
        "undo ip route-static default-preference"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<preference>",
            "Info": "Specifies the default priority for IPv4 static routes.\nIt is an integer ranging from 1 to 255."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ip route-static default-preference 70"
        ]
    ],
    "ExtraInfo": "You can change the status of a route by changing its priority. To change the default priority of the new IPv4 static routes, run the ip route-static default-preference command."
}