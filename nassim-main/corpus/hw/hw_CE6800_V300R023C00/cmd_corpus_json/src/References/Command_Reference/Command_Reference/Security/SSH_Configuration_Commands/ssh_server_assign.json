{
    "PageTitle": "ssh server assign",
    "FuncDef": "The ssh server assign command assigns a host key or PKI certificate to an SSH server.\nThe undo ssh server assign command deletes a host key or PKI certificate assigned to an SSH server. The ssh server assign command assigns a host key or PKI certificate to an SSH server. The undo ssh server assign command deletes a host key or PKI certificate assigned to an SSH server. By default, no key or PKI certificate is assigned to an SSH server.",
    "CLIs": [
        "ssh server assign { rsa-host-key <key-name> | dsa-host-key <key-name> | ecc-host-key <key-name> | sm2-host-key <key-name> | pki <key-name> }",
        "undo ssh server assign rsa-host-key",
        "undo ssh server assign dsa-host-key",
        "undo ssh server assign ecc-host-key",
        "undo ssh server assign sm2-host-key",
        "undo ssh server assign pki"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "rsa-host-key <key-name>",
            "Info": "Assigns an RSA host key to an SSH server and specifies the name of the RSA host key.\nThe value is a string of 1 to 35 case-insensitive characters and can only contain digits, letters, and underscores (_)."
        },
        {
            "Parameters": "dsa-host-key <key-name>",
            "Info": "Assigns a DSA host key to an SSH server and specifies the name of the DSA host key.\nThe value is a string of 1 to 35 case-insensitive characters and can only contain digits, letters, and underscores (_)."
        },
        {
            "Parameters": "ecc-host-key <key-name>",
            "Info": "Assigns an ECC host key to an SSH server and specifies the name of the ECC host key.\nThe value is a string of 1 to 35 case-insensitive characters and can only contain digits, letters, and underscores (_)."
        },
        {
            "Parameters": "sm2-host-key <key-name>",
            "Info": "Assigns an SM2 host key to an SSH server and specifies the name of the SM2 host key.\nThe value is a string of 1 to 35 case-insensitive characters and can only contain digits, letters, and underscores (_)."
        },
        {
            "Parameters": "pki <key-name>",
            "Info": "Assigns a PKI realm to an SSH server and specifies the name of the PKI realm.\nThe value is a string of 1 to 64 case-insensitive characters without spaces. If an initial certificate is loaded to the specified PKI realm, the certificate is delivered in interactive mode."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ssh server assign pki default",
            "Warning: A preset certificate is loaded to the specified PKI domain. The current operation has security risks. Continue? [Y/N]:y"
        ],
        [
            "<HUAWEI> system-view",
            "[*HUAWEI] ssh server assign pki domainA"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] sm2 key-pair label sm2key001",
            "[*HUAWEI] ssh server assign sm2-host-key sm2key001"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ecc key-pair label ecckey",
            "[*HUAWEI] ssh server assign ecc-host-key ecckey"
        ]
    ],
    "ExtraInfo": "You can run this command to reference the generated RSA, DSA, SM2, and ECC keys or assign a PKI certificate to the SSH server to ensure security of the SSH server."
}