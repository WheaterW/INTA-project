{
    "PageTitle": "import-route bgp permit-ibgp (RIPng view)",
    "FuncDef": "The import-route bgp permit-ibgp command configures RIPng to import routes from other routing protocols or RIPng processes.\nThe undo import-route bgp permit-ibgp command prevents RIPng from importing routes from other routing protocols or RIPng processes. The import-route bgp permit-ibgp command configures RIPng to import routes from other routing protocols or RIPng processes. The undo import-route bgp permit-ibgp command prevents RIPng from importing routes from other routing protocols or RIPng processes. By default, RIPng does not import routes from any other routing protocols or RIPng processes.",
    "CLIs": [
        "import-route bgp permit-ibgp [ [ cost <cost> | inherit-cost ] | [ route-policy <route-policy-name> ] ]",
        "undo import-route bgp permit-ibgp"
    ],
    "ParentView": [
        "RIPng view"
    ],
    "ParaDef": [
        {
            "Parameters": "cost <cost>",
            "Info": "Specifies the cost for imported routes.\nThe value is an integer ranging from 0 to 15. The default value is 0."
        },
        {
            "Parameters": "inherit-cost",
            "Info": "Retains the original cost of the imported route.\n-"
        },
        {
            "Parameters": "route-policy <route-policy-name>",
            "Info": "Specifies the name of a route-policy used to filter imported routes. A device imports only routes matching the policy.\nThe value is a string of 1 to 200 case-sensitive characters without spaces. The character string can contain spaces if it is enclosed with double quotation marks (\")."
        },
        {
            "Parameters": "bgp",
            "Info": "Imports BGP routes.\n-"
        },
        {
            "Parameters": "permit-ibgp",
            "Info": "Imports IBGP routes.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] route-policy abc permit node 10",
            "[*HUAWEI-route-policy] quit",
            "[*HUAWEI] ripng 1",
            "[*HUAWEI-ripng-1] import-route bgp permit-ibgp cost 5 route-policy abc"
        ]
    ],
    "ExtraInfo": "When RIPng and other routing protocols are deployed on a network, you can enable the traffic within a RIPng domain to reach a destination outside the RIPng domain using import-route command."
}