{
    "PageTitle": "graceful-shutdown manual-activate",
    "FuncDef": "The graceful-shutdown manual-activate command activates the g-shut feature globally.\nThe undo graceful-shutdown manual-activate command restores the default configuration. The graceful-shutdown manual-activate command activates the g-shut feature globally. The undo graceful-shutdown manual-activate command restores the default configuration. By default, the g-shut feature is not activated globally.",
    "CLIs": [
        "graceful-shutdown manual-activate",
        "undo graceful-shutdown manual-activate"
    ],
    "ParentView": [
        "BGP view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] graceful-shutdown manual-activate"
        ]
    ],
    "ExtraInfo": "To activate the g-shut feature for all peers, run this command."
}