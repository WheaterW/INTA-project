{
    "PageTitle": "if-match ip next-hop ip-prefix",
    "FuncDef": "The if-match ip next-hop ip-prefix command configures a filtering rule that is based on next-hop addresses of IPv4 routes.\nThe undo if-match ip next-hop ip-prefix command cancels the configuration. The if-match ip next-hop ip-prefix command configures a filtering rule that is based on next-hop addresses of IPv4 routes. The undo if-match ip next-hop ip-prefix command cancels the configuration. By default, no filtering rule based on next-hop IP addresses is configured.",
    "CLIs": [
        "if-match ip next-hop ip-prefix <ip-prefix-name>",
        "undo if-match ip next-hop [ ip-prefix <ip-prefix-name> ]"
    ],
    "ParentView": [
        "Route-policy view"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-prefix <ip-prefix-name>",
            "Info": "Specifies the name of an IP prefix list.\nThe value is a string of 1 to 169 case-sensitive characters, with spaces not supported. When double quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ip ip-prefix p1 permit 10.0.192.0 8 greater-equal 17 less-equal 18",
            "[*HUAWEI] route-policy policy permit node 10",
            "[*HUAWEI-route-policy] if-match ip next-hop ip-prefix p1"
        ]
    ],
    "ExtraInfo": "The if-match ip next-hop ip-prefix command is widely used. To make the matching condition take effect, the if-match ip next-hop ip-prefix command must be used together with the IP prefix list. For example:"
}