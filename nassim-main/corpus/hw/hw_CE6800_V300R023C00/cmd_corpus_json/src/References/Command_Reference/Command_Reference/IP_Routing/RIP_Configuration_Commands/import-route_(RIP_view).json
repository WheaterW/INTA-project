{
    "PageTitle": "import-route (RIP view)",
    "FuncDef": "The import-route command imports other routing protocol routes to RIP.\nThe undo import-route command deletes other routing protocol routes from RIP. The import-route command imports other routing protocol routes to RIP. The undo import-route command deletes other routing protocol routes from RIP. By default, RIP does not import routes from any other processes or routing protocols.",
    "CLIs": [
        "import-route { { static | direct | bgp } | { { rip | ospf | isis } [ <process-id> ] } } [ [ cost <cost> ] | [ route-policy <route-policy-name> ] ]",
        "import-route bgp { costtransparentroute-policy <route-policy-name> | route-policy <route-policy-name> costtransparent | costtransparent }",
        "undo import-route { { direct | static | bgp } | { { ospf | rip | isis } [ <process-id> ] } }"
    ],
    "ParentView": [
        "RIP view"
    ],
    "ParaDef": [
        {
            "Parameters": "static",
            "Info": "Imports static routes.\n-"
        },
        {
            "Parameters": "direct",
            "Info": "Imports direct routes.\n-"
        },
        {
            "Parameters": "bgp",
            "Info": "Imports BGP routes.\n-"
        },
        {
            "Parameters": "rip",
            "Info": "Imports RIP routes.\n-"
        },
        {
            "Parameters": "ospf",
            "Info": "Imports OSPF routes.\n-"
        },
        {
            "Parameters": "isis",
            "Info": "Imports IS-IS routes.\n-"
        },
        {
            "Parameters": "<process-id>",
            "Info": "Specifies the ID of an IS-IS, OSPF, or RIP process.\nA device can import RIP routes from other processes with an ID different from the local one.\nThe value is an integer ranging from 1 to 4294967295. The default value is 1."
        },
        {
            "Parameters": "cost <cost>",
            "Info": "Specifies the cost for imported routes.\nThe value is an integer ranging from 0 to 15. The default value is 0."
        },
        {
            "Parameters": "route-policy <route-policy-name>",
            "Info": "Specifies the name of a route-policy used to filter imported routes. A device imports only routes matching the policy.\nThe value is a string of 1 to 200 case-sensitive characters without spaces. The character string can contain spaces if it is enclosed with double quotation marks (\")."
        },
        {
            "Parameters": "transparent",
            "Info": "Retains the Multi_Exit Discriminator (MED) value of an imported BGP route as its cost. transparent takes effect only when the protocol is BGP.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] rip 1",
            "[*HUAWEI-rip-1] import-route isis 7 cost 5"
        ]
    ],
    "ExtraInfo": "When RIP and other routing protocols are deployed on a network, you can enable the traffic within a RIP domain to reach a destination outside the RIP domain using the import-route command."
}