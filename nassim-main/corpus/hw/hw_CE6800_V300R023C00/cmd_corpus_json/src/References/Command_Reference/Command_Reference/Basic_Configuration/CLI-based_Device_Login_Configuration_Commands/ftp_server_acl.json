{
    "PageTitle": "ftp server acl",
    "FuncDef": "The ftp server acl command configures the ACL to control the access of clients to the FTP server.\nThe undo ftp server acl command cancels the configuration of the ACL. The ftp server acl command configures the ACL to control the access of clients to the FTP server. The undo ftp server acl command cancels the configuration of the ACL. By default, no ACL is configured for FTP server.",
    "CLIs": [
        "ftp server acl { <acl-number> | <name> }",
        "undo ftp server acl"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<acl-number>",
            "Info": "Specifies the basic or advanced ACL number.\nACL number is an integer data type. The basic acl number value ranges from 2000 to 2999, the advanced acl number value ranges from 3000 to 3999."
        },
        {
            "Parameters": "<name>",
            "Info": "Specifies the ACL name.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported. The value must start with a letter (a to z or A to Z) and cannot contain only digits."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl name acl1",
            "[*HUAWEI-acl4-advance-acl1] quit",
            "[*HUAWEI] ftp server acl acl1"
        ]
    ],
    "ExtraInfo": "When a device functions as an FTP server, you can configure an ACL on the device to control the clients that can log in to the device through FTP.\nThe FTP server supports ACL filtering only based on the source IP address, destination IP address, source port number, destination port number, and VPN parameters.\nFor details about ACL configuration commands, see ACL4 Configuration Commands in the Command Reference - IP Addresses and Services."
}