{
    "PageTitle": "default (OSPF view)",
    "FuncDef": "The default command configures default parameters for OSPF to import external routes. The parameters include the cost, type (Type 1 or Type 2), and tag.\nThe undo default command restores the default setting. The default command configures default parameters for OSPF to import external routes. The parameters include the cost, type (Type 1 or Type 2), and tag. The undo default command restores the default setting. By default, the default cost of the external routes is 1; the type of the imported external routes is Type 2; the default tag value is 1.",
    "CLIs": [
        "default { cost { <costvalue> | inherit-metric } | tag <tagvalue> | type <typevalue> }",
        "undo default { cost [ { <costvalue> | inherit-metric } ] | tag [ <tagvalue> ] | type [ <typevalue> ] }"
    ],
    "ParentView": [
        "OSPF view"
    ],
    "ParaDef": [
        {
            "Parameters": "cost <costvalue>",
            "Info": "Specifies the default cost of the external routes imported by OSPF.\nThe value is an integer ranging from 0 to 16777214. The default value is 1."
        },
        {
            "Parameters": "inherit-metric",
            "Info": "Indicates that the cost of the imported route is the one carried in the route. If no cost is specified, the default cost set using the default command is used.\n-"
        },
        {
            "Parameters": "tag <tagvalue>",
            "Info": "Specifies the tag of the external routes.\nThe value is an integer ranging from 0 to 4294967295. The default value is 1."
        },
        {
            "Parameters": "type <typevalue>",
            "Info": "Specifies the type of the external routes.\nThe value is 1 or 2.\n1: Type 1 external route2: Type 2 external route\nThe default value is 2."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ospf 100",
            "[*HUAWEI-ospf-100] default cost 10 tag 100 type 2"
        ]
    ],
    "ExtraInfo": "The imported external routes carry various parameters that can change the priorities and next hops of those routes in the OSPF routing table.\nBy setting default parameters for OSPF to import external routes, you can control OSPF route selection.\nThe route tag is used to identify protocol-related information. For example, it can be used to differentiate AS numbers when OSPF receives BGP routes. In addition, configuring tag can filter OSPF routes carrying tags."
}