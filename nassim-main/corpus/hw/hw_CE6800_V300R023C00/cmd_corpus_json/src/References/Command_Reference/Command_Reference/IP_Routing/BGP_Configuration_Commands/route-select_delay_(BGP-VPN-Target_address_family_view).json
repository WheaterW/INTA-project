{
    "PageTitle": "route-select delay (BGP-VPN-Target address family view)",
    "FuncDef": "The route-select delay command configures a route selection delay.\nThe undo route-select delay command deletes the configured route selection delay. The route-select delay command configures a route selection delay. The undo route-select delay command deletes the configured route selection delay. The default delay-value value is 0, indicating that route selection is not delayed.",
    "CLIs": [
        "route-select delay <delay-value>",
        "undo route-select delay"
    ],
    "ParentView": [
        "BGP-VPN-target address family view"
    ],
    "ParaDef": [
        {
            "Parameters": "<delay-value>",
            "Info": "Specifies a route selection delay.\nThe value is an integer ranging from 0 to 3600, in seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] ipv4-family vpn-target",
            "[*HUAWEI-bgp-af-vpn-target] route-select delay 300"
        ]
    ],
    "ExtraInfo": "In a scenario in which primary and backup routes exist, packets may get lost after traffic switches back to the primary path from the backup path.\nYou can run the route-select delay command to configure a route selection delay. After the primary path recovers, an appropriate delay ensures that traffic switches back to the primary path after the device on the primary link completes refreshing forwarding entries."
}