{
    "PageTitle": "display dhcp relay statistics",
    "FuncDef": "The display dhcp relay statistics command displays message statistics on DHCP relay agents connected to DHCP servers in a specified DHCP server group. The display dhcp relay statistics command displays message statistics on DHCP relay agents connected to DHCP servers in a specified DHCP server group.",
    "CLIs": [
        "display dhcp relay statistics [ server-group <dhcp-server-group-name> ]"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "server-group <dhcp-server-group-name>",
            "Info": "Displays message statistics on DHCP relay agents connected to DHCP servers in a specified DHCP server group.\nIf this parameter is not specified, message statistics on DHCP relay agents connected to all DHCP servers are displayed.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported, and can contain only digits, letters, and special characters ('_', '-', and '.') and cannot use \"-\" and \"--\" as names."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display dhcp relay statistics"
        ]
    ],
    "ExtraInfo": "Run the display dhcp relay statistics command to check whether the client is correctly configured or the network is connected."
}