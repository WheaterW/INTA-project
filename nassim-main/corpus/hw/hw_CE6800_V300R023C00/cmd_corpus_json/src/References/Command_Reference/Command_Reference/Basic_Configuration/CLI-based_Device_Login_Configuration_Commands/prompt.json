{
    "PageTitle": "prompt",
    "FuncDef": "The prompt command enables the prompt message to get the confirmation from the user to transfer the file or not.\nThe undo prompt command disables the prompt. The prompt command enables the prompt message to get the confirmation from the user to transfer the file or not. The undo prompt command disables the prompt. By default, the prompt is disabled.",
    "CLIs": [
        "prompt",
        "undo prompt"
    ],
    "ParentView": [
        "FTP client view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> ftp 10.1.1.1",
            "[ftp] prompt"
        ]
    ],
    "ExtraInfo": "This command is applicable only for multiple file transfer commands mput and mget. In case of mget command, if the file already exists in the local device, system prompts the confirmation message whether to overwrite the file or not, irrespective of prompt is disabled or not."
}