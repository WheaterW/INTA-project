{
    "PageTitle": "if-match any",
    "FuncDef": "The if-match any command configures a matching rule based on all data packets in a traffic classifier.\nThe undo if-match any command deletes a matching rule based on all data packets in a traffic classifier. The if-match any command configures a matching rule based on all data packets in a traffic classifier. The undo if-match any command deletes a matching rule based on all data packets in a traffic classifier. By default, a matching rule based on all data packets is not configured in a traffic classifier.",
    "CLIs": [
        "if-match any",
        "undo if-match any"
    ],
    "ParentView": [
        "Traffic classifier view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] traffic classifier c1 type and",
            "[*HUAWEI-classifier-c1] if-match any"
        ]
    ],
    "ExtraInfo": "To process all the data packets in the same manner, run the if-match any command."
}