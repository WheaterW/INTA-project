{
    "PageTitle": "display arp (All views)",
    "FuncDef": "The display arp command displays the Address Resolution Protocol (ARP) entries. The display arp command displays the Address Resolution Protocol (ARP) entries.",
    "CLIs": [
        "display arp [ network <ip-address> [ <ip-mask> | <mask-length> ] ] [ dynamic | static ]"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "network <ip-address>",
            "Info": "Displays ARP entries based on the network ID expressed in dotted decimal notation.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "<ip-mask>",
            "Info": "Displays ARP entries based on the subnet mask of a specified network ID. The format of the subnet mask is X.X.X.X.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "<mask-length>",
            "Info": "Displays ARP entries based on the subnet mask length of a specified network ID.\nThe value ranges from 1 to 32"
        },
        {
            "Parameters": "dynamic",
            "Info": "Displays dynamic ARP entries.\n-"
        },
        {
            "Parameters": "static",
            "Info": "Displays static ARP entries.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display arp"
        ]
    ],
    "ExtraInfo": "To check ARP entries or locate ARP faults on an interface board, run the display arp slot command."
}