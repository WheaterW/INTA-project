{
    "PageTitle": "snmp-agent password min-length",
    "FuncDef": "The snmp-agent password min-length command configures the minimum SNMP password length.\nThe undo snmp-agent password min-length command restores the default minimum SNMP password length. The snmp-agent password min-length command configures the minimum SNMP password length. The undo snmp-agent password min-length command restores the default minimum SNMP password length. By default, the minimum SNMP password length is 8 bytes.",
    "CLIs": [
        "snmp-agent password min-length <min-length>",
        "undo snmp-agent password min-length"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<min-length>",
            "Info": "Specifies the minimum SNMP password length.\nThe value is an integer ranging from 8 to 16."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] snmp-agent password min-length 10"
        ]
    ],
    "ExtraInfo": "To improve system security and prevent password theft, run the snmp-agent password min-length command to configure the minimum SNMP password length. After this command is run, the length of a configured SNMP password must be longer than or equal to the minimum SNMP password length.\nSNMP passwords consist of the authentication and encryption passwords of local and SNMP users as well as communities."
}