{
    "PageTitle": "network (OSPF area view)",
    "FuncDef": "The network command specifies the interface that runs OSPF and the area to which the interface belongs.\nThe undo network command disables OSPF from an interface. The network command specifies the interface that runs OSPF and the area to which the interface belongs. The undo network command disables OSPF from an interface. By default, the interface does not belong to any area.",
    "CLIs": [
        "network <address> <wildcard-mask>",
        "network <address> <wildcard-mask> description <text>",
        "undo network <address> <wildcard-mask>"
    ],
    "ParentView": [
        "OSPF area view"
    ],
    "ParaDef": [
        {
            "Parameters": "<address>",
            "Info": "Specifies the address of a network segment where an interface resides.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "<wildcard-mask>",
            "Info": "Specifies the wildcard mask of an IP address, which is similar to the reversed form of an IP address mask. \"1\" represents that the corresponding bit in the IP address is ignored, and \"0\" represents that this bit must be reserved.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "description <text>",
            "Info": "Specifies the description of an OSPF network segment.\nThe value is a string of 1 to 80 case-sensitive characters, spaces supported."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ospf 100",
            "[*HUAWEI-ospf-100] area 2",
            "[*HUAWEI-ospf-100-area-0.0.0.2] network 192.168.1.0 0.0.0.255 description this network is connected to Beijing"
        ]
    ],
    "ExtraInfo": "Using the address and wildcard-mask parameters, you can configure one or more interfaces in an area.\nOSPF can run on an interface only when the following conditions are met:"
}