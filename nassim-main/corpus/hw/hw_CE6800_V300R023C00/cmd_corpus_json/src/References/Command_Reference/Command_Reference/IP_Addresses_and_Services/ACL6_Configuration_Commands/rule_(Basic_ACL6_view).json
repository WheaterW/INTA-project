{
    "PageTitle": "rule (Basic ACL6 view)",
    "FuncDef": "The rule command creates or modifies an ACL6 rule in the basic ACL6 view.\nThe undo rule command deletes an ACL6 rule in the basic ACL6 view. The rule command creates or modifies an ACL6 rule in the basic ACL6 view. The undo rule command deletes an ACL6 rule in the basic ACL6 view. By default, no basic ACL6 rule is created.",
    "CLIs": [
        "rule [ <rule-id> ] [ name <rule-name> ] { permit | deny } [ fragment | source { <source-ipv6-address> { <prefix-length> | <source-wildcard> } | <source-ipv6-address/prefix-length> | any } | time-range <time-name> | vpn-instance <vpn-instance-name> | logging ]",
        "undo rule [ name <rule-name> ] { permit | deny } [ fragment | source { <source-ipv6-address> { <prefix-length> | <source-wildcard> } | <source-ipv6-address/prefix-length> | any } | time-range <time-name> | [ vpn-instance <vpn-instance-name> ] | logging ]",
        "undo rule <rule-id> [ to <end-rule-id> ]"
    ],
    "ParentView": [
        "Basic ACL6 view"
    ],
    "ParaDef": [
        {
            "Parameters": "<rule-id>",
            "Info": "Specifies the ID of an ACL6 rule.\nThe value is an integer ranging from 0 to 4294967294."
        },
        {
            "Parameters": "name <rule-name>",
            "Info": "Specifies the name of an ACL rule.\nThe value is a string of 1 to 32 case-sensitive characters. It cannot contain spaces or start with an underscore (_)."
        },
        {
            "Parameters": "permit",
            "Info": "Permits packets that match conditions.\n-"
        },
        {
            "Parameters": "deny",
            "Info": "Denies the packets that match the rule.\n-"
        },
        {
            "Parameters": "fragment",
            "Info": "Checks fragmented packets.\n-"
        },
        {
            "Parameters": "source",
            "Info": "Matches packets based on source IPv6 addresses.\nIf source is not configured, packets from any source IPv6 address are matched.\n-"
        },
        {
            "Parameters": "<source-ipv6-address>",
            "Info": "Specifies  the source IPv6 address.\nThe value is a 32-digit hexadecimal number, in the format of X:X:X:X:X:X:X:X."
        },
        {
            "Parameters": "<prefix-length>",
            "Info": "Specifies the length of an IPv6 address mask.\nThe value is an integer ranging from 1 to 128."
        },
        {
            "Parameters": "<source-wildcard>",
            "Info": "Specifies the wildcard mask of a source IPv6 address.\nThe value is a 32-digit hexadecimal number, in the format of X:X:X:X:X:X:X:X."
        },
        {
            "Parameters": "<source-ipv6-address/prefix-length>",
            "Info": "Specifies the IPv6 address and mask length of the source IPv6 address.\nThe address is a 32-digit hexadecimal number, in the format of X:X:X:X:X:X:X:X.The value of mask length is an integer ranging from 1 to 128."
        },
        {
            "Parameters": "any",
            "Info": "Matches packets with any source IPv6 address.\n-"
        },
        {
            "Parameters": "time-range <time-name>",
            "Info": "Specifies a time range during which a basic ACL6 rule takes effect.\nA time range is configured using the time-range command.\nThe value is a string of 1 to 32 case-sensitive characters. It cannot contain spaces."
        },
        {
            "Parameters": "vpn-instance <vpn-instance-name>",
            "Info": "Matches packets based on an IPv6 VPN instance name. If the traffic is from L3VPN, this option must be configured in the ACL. If this option is not configured, it indicates the traffic belongs to the public network rather than L3VPN.\nThe value is a string of 1 to 31 case-sensitive characters. It cannot contain spaces. The VPN instance name cannot be _public_. If the character string is quoted by double quotation marks, the character string can contain spaces."
        },
        {
            "Parameters": "logging",
            "Info": "Indicates whether to log the matched packets.\n-"
        },
        {
            "Parameters": "rule",
            "Info": "Specifies an ACL6 rule.\n-"
        },
        {
            "Parameters": "to <end-rule-id>",
            "Info": "Specifies an end rule ID for advanced ACL6 rules to be deleted in batches.\nThe value is an integer ranging from 0 to 4294967294."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl ipv6 number 2999",
            "[*HUAWEI-acl6-basic-2999] rule permit source 2001:db8::1/64"
        ]
    ],
    "ExtraInfo": "After a basic ACL6 is created, run the rule command to add rules to the ACL6."
}