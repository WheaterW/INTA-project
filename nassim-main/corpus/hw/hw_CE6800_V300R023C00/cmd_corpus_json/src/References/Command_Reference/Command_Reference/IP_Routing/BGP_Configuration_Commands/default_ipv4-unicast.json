{
    "PageTitle": "default ipv4-unicast",
    "FuncDef": "The default ipv4-unicast command enables the IPv4 unicast address family for all peers.\nThe undo default ipv4-unicast command disables the IPv4 unicast address family for all peers. The default ipv4-unicast command enables the IPv4 unicast address family for all peers. The undo default ipv4-unicast command disables the IPv4 unicast address family for all peers. By default, the IPv4 unicast address family is enabled.",
    "CLIs": [
        "default ipv4-unicast",
        "undo default ipv4-unicast"
    ],
    "ParentView": [
        "BGP view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] undo default ipv4-unicast"
        ]
    ],
    "ExtraInfo": "If the IPv4 unicast address family needs to be enabled by default for created BGP peers, run the default ipv4-unicast command to set the default address family of BGP to the IPv4 unicast address family.\nIf the IPv4 unicast address family does not need to be enabled by default for created BGP peers, run the undo default ipv4-unicast command to disable the IPv4 unicast address family for all peers."
}