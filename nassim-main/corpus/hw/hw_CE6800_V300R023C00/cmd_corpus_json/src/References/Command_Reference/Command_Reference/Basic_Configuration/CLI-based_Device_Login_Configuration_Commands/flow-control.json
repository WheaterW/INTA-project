{
    "PageTitle": "flow-control",
    "FuncDef": "The flow-control command configures a flow control mode.\nThe undo flow-control command restores the default flow control mode. The flow-control command configures a flow control mode. The undo flow-control command restores the default flow control mode. By default, the flow control mode is set to none, indicating that traffic is not controlled.",
    "CLIs": [
        "flow-control <flowtype>",
        "undo flow-control"
    ],
    "ParentView": [
        "VTY-type user interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "<flowtype>",
            "Info": "Specifies the flow control mode.\nThe value is an enumerated type, which can be:\nnone: Implements no flow control.hardware: Implements hardware-based flow control.software: Implements software-based flow control."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] user-interface console 0",
            "[~HUAWEI-ui-console0] flow-control software"
        ]
    ],
    "ExtraInfo": "Flow control prevents packet loss caused by network congestion. You can run this command to control the packet rate on a console interface."
}