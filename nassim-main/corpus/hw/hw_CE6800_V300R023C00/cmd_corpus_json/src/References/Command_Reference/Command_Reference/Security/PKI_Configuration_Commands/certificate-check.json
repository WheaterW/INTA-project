{
    "PageTitle": "certificate-check",
    "FuncDef": "The certificate-check command sets the method of checking whether a certificate in the PKI realm is revoked.\nThe undo certificate-check command cancels the method of checking whether a certificate in the PKI realm is revoked. The certificate-check command sets the method of checking whether a certificate in the PKI realm is revoked. The undo certificate-check command cancels the method of checking whether a certificate in the PKI realm is revoked. By default, the system does not check whether a certificate in the PKI realm is revoked.",
    "CLIs": [
        "certificate-check { { crl | ocsp } [ none ] | none }",
        "undo certificate-check"
    ],
    "ParentView": [
        "PKI domain view"
    ],
    "ParaDef": [
        {
            "Parameters": "crl",
            "Info": "Sets the check method to Certificate Revocation List (CRL).\n-"
        },
        {
            "Parameters": "ocsp",
            "Info": "Sets the check method to Online Certificate Status Protocol (OCSP).\n-"
        },
        {
            "Parameters": "none",
            "Info": "Indicates that the system does not check whether a certificate is revoked.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] pki realm test",
            "[*HUAWEI-pki-realm-test] certificate-check crl none"
        ]
    ],
    "ExtraInfo": "When a PKI entity verifies the peer certificate, it often needs to check whether the peer certificate is valid, for example, whether the peer certificate has expired or has been added to the CRL. In this case, you can run this command to check the peer certificate status.\nThe following methods can be used to check the certificate revocation status in a PKI realm:"
}