{
    "PageTitle": "ssh server acl",
    "FuncDef": "The ssh server acl command configures the ACL to control the access of clients for STelnet, SFTP, SCP and SNETCONF.\nThe undo ssh server acl command cancels the ACL configuration. The ssh server acl command configures the ACL to control the access of clients for STelnet, SFTP, SCP and SNETCONF. The undo ssh server acl command cancels the ACL configuration. By default, no ACL is configured.",
    "CLIs": [
        "ssh server acl { <acl4name> | <acl4num> }",
        "ssh ipv6 server acl { <acl6name> | <acl6num> }",
        "undo ssh server acl",
        "undo ssh ipv6 server acl"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<acl4name>",
            "Info": "Specifies the ACL4 name.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported."
        },
        {
            "Parameters": "<acl4num>",
            "Info": "Specifies the IPv4 ACL number.\nACL number is an integer data type. The basic acl number value ranges from 2000 to 2999, the advanced acl number value ranges from 3000 to 3999."
        },
        {
            "Parameters": "ipv6",
            "Info": "IPv6 protocol.\n-"
        },
        {
            "Parameters": "<acl6name>",
            "Info": "Specify the ACL6 name.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported."
        },
        {
            "Parameters": "<acl6num>",
            "Info": "Specifies the ACL6 number.\nACL number is an integer data type. The basic acl number value ranges from 2000 to 2999, the advanced acl number value ranges from 3000 to 3999."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl 2000",
            "[*HUAWEI-acl4-basic-2000] quit",
            "[*HUAWEI] ssh server acl 2000"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl ipv6 name test",
            "[*HUAWEI-acl6-advance-test] quit",
            "[*HUAWEI] ssh ipv6 server acl test"
        ]
    ],
    "ExtraInfo": "If a device serves as"
}