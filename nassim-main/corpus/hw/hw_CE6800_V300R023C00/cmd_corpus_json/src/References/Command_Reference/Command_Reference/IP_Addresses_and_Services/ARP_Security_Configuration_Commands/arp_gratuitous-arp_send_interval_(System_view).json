{
    "PageTitle": "arp gratuitous-arp send interval (System view)",
    "FuncDef": "The arp gratuitous-arp send interval command sets an interval at which a device sends gratuitous Address Resolution Protocol (ARP) packets.\nThe undo arp gratuitous-arp send interval command restores the default interval at which a device sends gratuitous ARP packets. The arp gratuitous-arp send interval command sets an interval at which a device sends gratuitous Address Resolution Protocol (ARP) packets. The undo arp gratuitous-arp send interval command restores the default interval at which a device sends gratuitous ARP packets. By default, a device sends gratuitous ARP packets at an interval of 60s.",
    "CLIs": [
        "arp gratuitous-arp send interval <interval>",
        "undo arp gratuitous-arp send interval [ <interval> ]"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<interval>",
            "Info": "Specifies an interval at which a device sends gratuitous ARP packets.\nThe value is an integer ranging from 1 to 86400, in seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] arp gratuitous-arp send interval 100"
        ]
    ],
    "ExtraInfo": "By default, after a device or interface is enabled to send gratuitous ARP packets, the device sends them at an interval of 60s. To change the interval, you can run the\narp gratuitous-arp send interval command."
}