{
    "PageTitle": "match ip",
    "FuncDef": "The match ip command configures aggregation keywords in an IPv4 flexible flow statistics template.\nThe undo match ip command deletes aggregation keywords from an IPv4 flexible flow statistics template. The match ip command configures aggregation keywords in an IPv4 flexible flow statistics template. The undo match ip command deletes aggregation keywords from an IPv4 flexible flow statistics template. By default, no aggregation keyword is configured in an IPv4 flexible flow statistics template.",
    "CLIs": [
        "match ip { destination-address | destination-port | tos | protocol | source-address | source-port | source-mac | destination-mac | ethernet-type | vlan }",
        "undo match ip { destination-address | destination-port | tos | protocol | source-address | source-port | source-mac | destination-mac | ethernet-type | vlan }"
    ],
    "ParentView": [
        "IPv4 flexible flow statistics template view"
    ],
    "ParaDef": [
        {
            "Parameters": "destination-address",
            "Info": "Indicates that flows are aggregated based on destination addresses.\n-"
        },
        {
            "Parameters": "destination-port",
            "Info": "Indicates that flows are aggregated based on destination ports.\n-"
        },
        {
            "Parameters": "tos",
            "Info": "Indicates that flows are aggregated based on ToS fields.\n-"
        },
        {
            "Parameters": "protocol",
            "Info": "Indicates that flows are aggregated based on protocol types.\n-"
        },
        {
            "Parameters": "source-address",
            "Info": "Indicates that flows are aggregated based on source addresses.\n-"
        },
        {
            "Parameters": "source-port",
            "Info": "Indicates that flows are aggregated based on source ports.\n-"
        },
        {
            "Parameters": "source-mac",
            "Info": "Indicates that flows are aggregated based on the source MAC address .\n-"
        },
        {
            "Parameters": "destination-mac",
            "Info": "Indicates that flows are aggregated based on the destination MAC address.\n-"
        },
        {
            "Parameters": "ethernet-type",
            "Info": "Indicates that flows are aggregated based on Ethernet type.\n-"
        },
        {
            "Parameters": "vlan",
            "Info": "Indicates that flows are aggregated based on VLAN.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] netstream record abc123 ip",
            "[*HUAWEI-netstream-record-ipv4-abc123] match ip source-port"
        ]
    ],
    "ExtraInfo": "During NetStream implementation, you can run the match ip command to configure aggregation keywords in a flexible flow statistics template."
}