{
    "PageTitle": "lldp management-address interface-mode",
    "FuncDef": "The lldp management-address interface-mode command configures a management address TLV to use a local interface address. After the lldp management-address command is run, the LLDP management address TLV carries the IP address of the local interface.\nThe undo lldp management-address interface-mode command disables the LLDP management address TLV from using a local interface address. The lldp management-address interface-mode command configures a management address TLV to use a local interface address. After the lldp management-address command is run, the LLDP management address TLV carries the IP address of the local interface. The undo lldp management-address interface-mode command disables the LLDP management address TLV from using a local interface address. By default, the management address is not configured to work in interface mode.",
    "CLIs": [
        "lldp management-address interface-mode",
        "undo lldp management-address interface-mode"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] lldp management-address interface-mode"
        ]
    ],
    "ExtraInfo": "The management address is carried in the Management Address TLV in an LLDP packet. The management address is used by the NMS to identify devices and implement network management. A management address uniquely identifies a device, helping the NMS to determine the network topology and management devices.\nTo enable the management address to be advertised to neighbors to be a local interface address, run the lldp management-address interface-mode command."
}