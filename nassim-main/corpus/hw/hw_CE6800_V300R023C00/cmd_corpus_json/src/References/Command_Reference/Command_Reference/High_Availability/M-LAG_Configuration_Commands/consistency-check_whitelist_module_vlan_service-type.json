{
    "PageTitle": "consistency-check whitelist module vlan service-type",
    "FuncDef": "The consistency-check whitelist module vlan service-type command configures the M-LAG consistency check whitelist for the VLAN module.\nThe undo consistency-check whitelist module vlan service-type command cancels the M-LAG consistency check whitelist configured for the VLAN module. The consistency-check whitelist module vlan service-type command configures the M-LAG consistency check whitelist for the VLAN module. The undo consistency-check whitelist module vlan service-type command cancels the M-LAG consistency check whitelist configured for the VLAN module. By default, the M-LAG consistency check whitelist is not configured for the VLAN module.",
    "CLIs": [
        "consistency-check whitelist module vlan service-type { vlan-configuration | port-vlan-relation }",
        "undo consistency-check whitelist module vlan service-type { vlan-configuration | port-vlan-relation }"
    ],
    "ParentView": [
        "DFS group view"
    ],
    "ParaDef": [
        {
            "Parameters": "vlan-configuration",
            "Info": "VLAN configuration.\n-"
        },
        {
            "Parameters": "port-vlan-relation",
            "Info": "VLANs of packets allowed to pass through interfaces.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] dfs-group 1",
            "[*HUAWEI-dfs-group-1] consistency-check whitelist module vlan service-type vlan-configuration"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] dfs-group 1",
            "[*HUAWEI-dfs-group-1] consistency-check whitelist module vlan service-type port-vlan-relation"
        ]
    ],
    "ExtraInfo": ""
}