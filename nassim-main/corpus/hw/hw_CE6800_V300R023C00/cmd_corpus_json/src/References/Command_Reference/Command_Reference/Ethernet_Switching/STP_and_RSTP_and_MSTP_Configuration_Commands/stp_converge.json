{
    "PageTitle": "stp converge",
    "FuncDef": "The stp converge command sets a convergence mode for a spanning tree protocol.\nThe undo stp converge command restores the default mode. The stp converge command sets a convergence mode for a spanning tree protocol. The undo stp converge command restores the default mode. By default, the convergence mode is normal.",
    "CLIs": [
        "stp converge { fast | normal }",
        "undo stp converge"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "fast",
            "Info": "Directly deletes ARP entries.\n-"
        },
        {
            "Parameters": "normal",
            "Info": "Ages ARP entries quickly.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] stp converge normal"
        ]
    ],
    "ExtraInfo": "On a Layer 2 network running STP, if the topology of a spanning tree instance changes, the forwarding paths of VLANs that are mapped to this instance change. As a result, ARP entries related to these VLANs need to be updated. Two methods are available:"
}