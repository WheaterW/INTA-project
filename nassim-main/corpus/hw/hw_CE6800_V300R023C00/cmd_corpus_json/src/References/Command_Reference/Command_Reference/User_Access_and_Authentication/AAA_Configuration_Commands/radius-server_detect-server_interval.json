{
    "PageTitle": "radius-server detect-server interval",
    "FuncDef": "The radius-server detect-server interval command configures an automatic detection interval for RADIUS servers in Down status. The undo radius-server detect-server interval command restores the default settings. The default automatic detection interval is 60 seconds.",
    "CLIs": [
        "radius-server detect-server interval <interval>",
        "undo radius-server detect-server interval"
    ],
    "ParentView": [
        "RADIUS server template view"
    ],
    "ParaDef": [
        {
            "Parameters": "<interval>",
            "Info": "Specifies the automatic detection interval for RADIUS servers in Down status.\nThe value is an integer that ranges from 5 to 3600. The default value is 60."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] radius-server template acs",
            "[*HUAWEI-radius-acs] radius-server detect-server interval 100"
        ]
    ],
    "ExtraInfo": ""
}