{
    "PageTitle": "supernet advertise (BGP-IPv4 unicast address family view)",
    "FuncDef": "The supernet unicast advertise enable command configures a BGP device to advertise BGP supernet unicast routes to its peers.\nThe undo supernet advertise command restores the default configuration. The supernet unicast advertise enable command configures a BGP device to advertise BGP supernet unicast routes to its peers. The undo supernet advertise command restores the default configuration. By default, BGP supernet unicast routes are considered invalid and are not advertised to peers or delivered to the IP routing table.",
    "CLIs": [
        "supernet unicast advertise enable",
        "supernet unicast advertise disable",
        "undo supernet unicast advertise enable"
    ],
    "ParentView": [
        "BGP-IPv4 unicast address family view,BGP view"
    ],
    "ParaDef": [
        {
            "Parameters": "unicast",
            "Info": "Indicates supernet unicast routes.\n-"
        },
        {
            "Parameters": "disable",
            "Info": "Disables the device from sending BGP supernet routes.\n-"
        },
        {
            "Parameters": "enable",
            "Info": "Enables the device to send BGP supernet routes.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] supernet unicast advertise enable"
        ]
    ],
    "ExtraInfo": "A BGP supernet route has the same destination address and next hop address or has a more detailed destination address than the next hop address. Any route that meets either of the following conditions is a BGP supernet route:"
}