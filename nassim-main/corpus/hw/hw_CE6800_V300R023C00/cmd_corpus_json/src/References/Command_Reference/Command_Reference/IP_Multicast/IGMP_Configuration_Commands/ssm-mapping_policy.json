{
    "PageTitle": "ssm-mapping policy",
    "FuncDef": "The ssm-mapping policy command configures an IPv4 SSM mapping policy.\nUsing the undo ssm-mapping policy command deletes an IPv4 SSM mapping policy. The ssm-mapping policy command configures an IPv4 SSM mapping policy. Using the undo ssm-mapping policy command deletes an IPv4 SSM mapping policy. By default, no IPv4 SSM mapping policy is configured.",
    "CLIs": [
        "ssm-mapping policy <policy-name>",
        "undo ssm-mapping policy <policy-name>"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<policy-name>",
            "Info": "Sets an SSM mapping policy name.\nThe name is a string of 1 to 31 characters and case sensitive."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ssm-mapping policy ssmmap1"
        ]
    ],
    "ExtraInfo": "When users connected to different interfaces want to watch programs multicast by different multicast sources, run the ssm-mapping policy command to configure different SSM mapping policies on different interfaces. After that, IGMP packets received by different interfaces can be mapped to different multicast sources and users' requirements can be answered."
}