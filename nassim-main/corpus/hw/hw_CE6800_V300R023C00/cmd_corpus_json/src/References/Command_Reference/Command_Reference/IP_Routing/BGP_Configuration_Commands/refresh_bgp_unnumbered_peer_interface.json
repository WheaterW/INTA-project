{
    "PageTitle": "refresh bgp unnumbered peer interface",
    "FuncDef": "The refresh bgp unnumbered peer interface command softly resets a BGP unnumbered peer connection.\nThe refresh bgp ipv6 unnumbered peer interface command softly resets a BGP IPv6 unnumbered peer connection. The refresh bgp unnumbered peer interface command softly resets a BGP unnumbered peer connection. The refresh bgp ipv6 unnumbered peer interface command softly resets a BGP IPv6 unnumbered peer connection.",
    "CLIs": [
        "refresh bgp unnumbered peer interface { <interface-name> | <IfType> <IFNum> } { export | import }",
        "refresh bgp ipv6 unnumbered peer interface { <interface-name> | <IfType> <IFNum> } { export | import }"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "<interface-name>",
            "Info": "Specifies an interface name.\n-"
        },
        {
            "Parameters": "<IfType>",
            "Info": "Specifies the type of an interface.\n-"
        },
        {
            "Parameters": "<IFNum>",
            "Info": "Specifies an interface number.\nThe value is a string of 1 to 63  case-sensitive characters, spaces not supported."
        },
        {
            "Parameters": "export",
            "Info": "Triggers outbound soft resetting.\n-"
        },
        {
            "Parameters": "import",
            "Info": "Triggers inbound soft resetting.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> refresh bgp unnumbered peer interface 100GE 1/0/8 import"
        ]
    ],
    "ExtraInfo": "If a device's peer supports route-refresh, the refresh bgp unnumbered peer interface command can be used on the device to softly reset the BGP unnumbered peer connection with the peer. BGP soft resetting can be used to refresh the BGP routing table and apply new routing policies, without closing any BGP peer connection."
}