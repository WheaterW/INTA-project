{
    "PageTitle": "bfd all-interfaces",
    "FuncDef": "The bfd all-interfaces command sets values for BFD session parameters.\nThe undo bfd all-interfaces command restores the default values. The bfd all-interfaces command sets values for BFD session parameters. The undo bfd all-interfaces command restores the default values. By default, the BFD session will set up with default values of parameters.",
    "CLIs": [
        "bfd all-interfaces { min-rx-interval <receive-interval> | min-tx-interval <transmit-interval> | detect-multiplier <multiplier-value> | frr-binding }",
        "undo bfd all-interfaces { min-rx-interval [ <receive-interval> ] | min-tx-interval [ <transmit-interval> ] | detect-multiplier [ <multiplier-value> ] | frr-binding }"
    ],
    "ParentView": [
        "IS-IS view"
    ],
    "ParaDef": [
        {
            "Parameters": "min-rx-interval <receive-interval>",
            "Info": "Specifies the expected minimum interval at which BFD packets are received from a neighbor.\nThe value is an integer ranging from 3 to 1000, in milliseconds. The default value is 1000."
        },
        {
            "Parameters": "min-tx-interval <transmit-interval>",
            "Info": "Specifies the minimum interval at which BFD packets are sent to a neighbor.\nThe value is an integer ranging from 3 to 1000, in milliseconds. The default value is 1000."
        },
        {
            "Parameters": "detect-multiplier <multiplier-value>",
            "Info": "Specifies a local detection multiplier.\nThe value is an integer ranging from 3 to 50. The default value is 3."
        },
        {
            "Parameters": "frr-binding",
            "Info": "Associates BFD session status with IS-IS auto FRR. If BFD detects a link fault on an interface, the BFD session goes Down, triggering FRR. Traffic is then quickly switched to the backup link, minimizing service loss.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] isis",
            "[*HUAWEI-isis-1] bfd all-interfaces enable",
            "[*HUAWEI-isis-1] bfd all-interfaces min-tx-interval 300"
        ]
    ],
    "ExtraInfo": "BFD can provide millisecond-level fault detection, help IS-IS to detect the faults that occur on neighboring devices or links more quickly, and instruct IS-IS to recalculate routes for correct packet forwarding. The bfd all-interfaces command can be used to set values for BFD session parameters."
}