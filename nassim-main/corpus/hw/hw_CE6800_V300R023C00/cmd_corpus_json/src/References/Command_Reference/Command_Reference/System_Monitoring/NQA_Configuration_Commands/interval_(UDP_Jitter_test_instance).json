{
    "PageTitle": "interval (UDP Jitter test instance)",
    "FuncDef": "The interval command sets the interval at which UDP Jitter test packets are sent.\nThe undo interval command restores the default interval. The interval command sets the interval at which UDP Jitter test packets are sent. The undo interval command restores the default interval. By default, test packets are sent at an interval of 20 ms.",
    "CLIs": [
        "interval milliseconds <interval>",
        "interval seconds <interval-mill>",
        "undo interval"
    ],
    "ParentView": [
        "NQA view"
    ],
    "ParaDef": [
        {
            "Parameters": "seconds <interval-mill>",
            "Info": "Specifies the interval at which test packets are sent, in seconds.\nThe value is an integer that ranges from 1 to 60, in seconds."
        },
        {
            "Parameters": "milliseconds <interval>",
            "Info": "Specifies the interval at which test packets are sent, in milliseconds.\nThe value is an integer ranging from 10 to 60000, in milliseconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] nqa test-instance user test",
            "[*HUAWEI-nqa-user-test] test-type jitter",
            "[*HUAWEI-nqa-user-test] interval seconds 30"
        ]
    ],
    "ExtraInfo": "The probe-count command sets the number of probes to be sent for an NQA test instance. Based on statistics about these probes, you can evaluate network quality. To set the interval at which test packets are sent in each probe, run the interval command."
}