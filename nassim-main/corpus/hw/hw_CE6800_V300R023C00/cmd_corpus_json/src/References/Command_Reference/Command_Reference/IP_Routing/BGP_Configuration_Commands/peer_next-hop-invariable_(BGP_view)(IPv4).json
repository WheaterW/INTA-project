{
    "PageTitle": "peer next-hop-invariable (BGP view)(IPv4)",
    "FuncDef": "The peer next-hop-invariable command configures the device not to change the next hop of a route when advertising the route to a peer group.\nThe undo peer next-hop-invariable command restores the default configuration. The peer next-hop-invariable command configures the device not to change the next hop of a route when advertising the route to a peer group. The undo peer next-hop-invariable command restores the default configuration. By default:",
    "CLIs": [
        "peer <ipv4-address> next-hop-invariable [ include-static-route | include-unicast-route ]",
        "undo peer <ipv4-address> next-hop-invariable"
    ],
    "ParentView": [
        "BGP-IPv4 unicast address family view,BGP view"
    ],
    "ParaDef": [
        {
            "Parameters": "<ipv4-address>",
            "Info": "Specifies the IPv4 address of a peer.\nIt is in dotted decimal notation."
        },
        {
            "Parameters": "include-static-route",
            "Info": "Configures the device to retain the original Next_Hop of static routes when advertising the routes to the peer.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] peer 10.1.1.2 as-number 100",
            "[*HUAWEI-bgp] peer 10.1.1.2 next-hop-invariable include-static-route"
        ]
    ],
    "ExtraInfo": "The peer next-hop-invariable command has the following functions:"
}