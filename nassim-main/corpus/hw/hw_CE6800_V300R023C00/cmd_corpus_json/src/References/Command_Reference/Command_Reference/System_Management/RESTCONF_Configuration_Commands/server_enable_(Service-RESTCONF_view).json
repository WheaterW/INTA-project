{
    "PageTitle": "server enable (Service-RESTCONF view)",
    "FuncDef": "The server enable command enables the RESTCONF service function based on HTTP protocol.\nThe undo server enable command disables the RESTCONF service function based on HTTP protocol.\nThe secure-server enable command enables the RESTCONF service function based on HTTPS protocol.\nThe undo secure-server enable command disables the RESTCONF service function based on HTTPS protocol. The server enable command enables the RESTCONF service function based on HTTP protocol. The undo server enable command disables the RESTCONF service function based on HTTP protocol. The secure-server enable command enables the RESTCONF service function based on HTTPS protocol. The undo secure-server enable command disables the RESTCONF service function based on HTTPS protocol. By default, the RESTCONF server function is disabled.",
    "CLIs": [
        "server enable",
        "secure-server enable",
        "undo server enable",
        "undo secure-server enable"
    ],
    "ParentView": [
        "Service-RESTCONF view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] http",
            "[*HUAWEI-http] service restconf",
            "[*HUAWEI-http-service-restconf] secure-server enable"
        ]
    ],
    "ExtraInfo": "RESTCONF, an HTTP/HTTPS-based protocol, provides RESTful programmatic interfaces and allows users to add, delete, modify, and query network device data.\nUsers can enable the RESTCONF function using this command."
}