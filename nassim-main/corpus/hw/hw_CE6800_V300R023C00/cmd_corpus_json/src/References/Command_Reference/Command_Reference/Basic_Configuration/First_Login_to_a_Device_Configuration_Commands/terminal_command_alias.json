{
    "PageTitle": "terminal command alias",
    "FuncDef": "The terminal command alias command enables the command alias function for the current terminal.\nThe undo terminal command alias disables the function. The terminal command alias command enables the command alias function for the current terminal. The undo terminal command alias disables the function. By default, the function is enabled.",
    "CLIs": [
        "terminal command alias",
        "undo terminal command alias"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> terminal command alias"
        ]
    ],
    "ExtraInfo": "The alias configured by the alias command can take effect only when the command alias function is enabled.\nIf you run the undo terminal command alias command to disable the command alias function for the current terminal, the command alias function can still be configured, and the configuration information of command alias is not deleted, but the alias configured cannot take effect."
}