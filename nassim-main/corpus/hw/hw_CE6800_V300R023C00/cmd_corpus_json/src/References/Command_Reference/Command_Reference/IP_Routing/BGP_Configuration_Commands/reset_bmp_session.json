{
    "PageTitle": "reset bmp session",
    "FuncDef": "The reset bmp session command resets a specified BGP Monitoring Protocol (BMP) session. The reset bmp session command resets a specified BGP Monitoring Protocol (BMP) session.",
    "CLIs": [
        "reset bmp session [ vpn-instance <vrf-name> ] { <ipv4Addr> | <ipv6Addr> | all }",
        "reset bmp session [ vpn-instance <vrf-name> ] { <ipv4Addr> | <ipv6Addr> } alias <alias-name>"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "vpn-instance <vrf-name>",
            "Info": "Specifies the name of a VPN instance.\nThe value is a string of 1 to 31 case-sensitive characters. It cannot contain spaces. The VPN instance name cannot be _public_. If the character string is quoted by double quotation marks, the character string can contain spaces."
        },
        {
            "Parameters": "<ipv4Addr>",
            "Info": "Resets a BMP session with an IPv4 session address.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "<ipv6Addr>",
            "Info": "Resets a BMP session with an IPv6 session address.\nThe value is a 32-digit hexadecimal number, in the format of X:X:X:X:X:X:X:X."
        },
        {
            "Parameters": "all",
            "Info": "Resets all BMP sessions.\n-"
        },
        {
            "Parameters": "alias <alias-name>",
            "Info": "Specifies a session alias. When the device needs to establish multiple TCP connections with the same monitoring server through different port numbers, specify session aliases for differentiation.\nThe value is a string of 1 to 31 case-sensitive characters without spaces. When double quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> reset bmp session 10.1.1.1"
        ],
        [
            "<HUAWEI> reset bmp session all"
        ],
        [
            "<HUAWEI> reset bmp session 2001:DB8:1::1"
        ],
        [
            "<HUAWEI> reset bmp session 10.1.1.1 alias aa"
        ]
    ],
    "ExtraInfo": "After configuring BMP session parameters, run the reset bmp session command to reset the BMP session for the new BMP session parameters to take effect.\nRun the following commands can change the BMP session configurations:"
}