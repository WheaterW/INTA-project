{
    "PageTitle": "databits",
    "FuncDef": "The databits command configures the number of data bits of a user interface.\nThe undo databits command restores the default number of data bits of a user interface. The databits command configures the number of data bits of a user interface. The undo databits command restores the default number of data bits of a user interface. By default, the default number of data bits of a user interface is 8.",
    "CLIs": [
        "databits <databitvalue>",
        "undo databits"
    ],
    "ParentView": [
        "VTY-type user interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "<databitvalue>",
            "Info": "Sets the number of data bits.\nThe value is an enumerated type, which is 5, 6, 7 or 8."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] user-interface console 0",
            "[~HUAWEI-ui-console0] databits 7"
        ]
    ],
    "ExtraInfo": "If the number of data bits configured on a device is different from that configured on the terminal emulation program, run the databits command to configure the number of data bits on the device to be the same as that on the terminal emulation program.\nYou are advised to use the default number of data bits of a user interface."
}