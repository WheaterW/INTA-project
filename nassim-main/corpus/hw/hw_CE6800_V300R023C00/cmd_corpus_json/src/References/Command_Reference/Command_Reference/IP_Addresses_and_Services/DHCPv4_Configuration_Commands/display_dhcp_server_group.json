{
    "PageTitle": "display dhcp server group",
    "FuncDef": "The display dhcp server group command displays the configuration of a DHCP server group. The display dhcp server group command displays the configuration of a DHCP server group.",
    "CLIs": [
        "display dhcp server group [ <group-name> ]"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "<group-name>",
            "Info": "Displays the configuration of a specified DHCP server group.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported, and can contain only digits, letters, and special characters ('_', '-', and '.') and cannot use \"-\" and \"--\" as names."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display dhcp server group myServers"
        ]
    ],
    "ExtraInfo": "This command applies to DHCP relay agents. The display dhcp server group command displays information about all the DHCP server groups of a DHCP relay agent and the number of DHCP servers in the DHCP server groups. If group-name is specified, the display dhcp server group group-name command displays DHCP server addresses and the number of DHCP servers in a specified DHCP server group."
}