{
    "PageTitle": "acl name (advanced ACL)",
    "FuncDef": "The acl name command creates an advanced ACL and displays the ACL view. If the advanced ACL to be created already exists, running this command directly displays the ACL view. The acl name command creates an advanced ACL and displays the ACL view. If the advanced ACL to be created already exists, running this command directly displays the ACL view. By default, no advanced ACL is created.",
    "CLIs": [
        "acl name <advance-acl-name> advance",
        "acl name <advance-acl-name> [ number ] <advance-acl-number>"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "advance",
            "Info": "Creates an advanced ACL with a keyword.\n-"
        },
        {
            "Parameters": "name <advance-acl-name>",
            "Info": "Creates an advanced ACL with a name.\nThe value is a string of 1 to 32 case-sensitive characters without spaces. The value must start with a letter or digit, and cannot contain only digits."
        },
        {
            "Parameters": "number <advance-acl-number>",
            "Info": "Creates an advanced ACL with a number.\nThe value is an integer ranging from 3000 to 3999."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl name advance-acl advance"
        ]
    ],
    "ExtraInfo": "Advanced ACLs match packets based on the source IP address, destination IP address, IP protocol type, and protocol-specific configurations (for example, source and destination TCP ports and ICMP protocol type and code) of the packets. To create an advanced ACL, run the acl command."
}