{
    "PageTitle": "bfd forwarding match remote-discriminator",
    "FuncDef": "The bfd forwarding match remote-discriminator command matches the local BFD discriminator configured on the peer end.\nThe undo bfd forwarding match remote-discriminator command disables a device from matching the local BFD discriminator configured on the peer. The bfd forwarding match remote-discriminator command matches the local BFD discriminator configured on the peer end. The undo bfd forwarding match remote-discriminator command disables a device from matching the local BFD discriminator configured on the peer. By default, no remote discriminator is configured for a BFD session.",
    "CLIs": [
        "bfd forwarding match remote-discriminator <discriminator-value> [ to <discriminator-value> ]",
        "undo bfd forwarding match remote-discriminator <discriminator-value> [ to <discriminator-value> ]"
    ],
    "ParentView": [
        "BFD view"
    ],
    "ParaDef": [
        {
            "Parameters": "<discriminator-value>",
            "Info": "Specifies a minimum remote discriminator for a BFD session.\nThe value is an integer ranging from 1 to 16384."
        },
        {
            "Parameters": "to <discriminator-value>",
            "Info": "Specifies a  maximum remote discriminator for a BFD session.\nThe value is an integer ranging from 1 to 16384."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bfd",
            "[*HUAWEI-bfd] bfd forwarding match remote-discriminator 11"
        ]
    ],
    "ExtraInfo": ""
}