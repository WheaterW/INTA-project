{
    "PageTitle": "local-aaa-user wrong-password",
    "FuncDef": "The local-aaa-user wrong-password command enables local account locking function and sets the retry interval, consecutive incorrect password attempts, and lockout duration. The undo local-aaa-user wrong-password command disables local account locking function. By default, the local account locking function is enabled, retry interval is 5 minutes, maximum number of consecutive incorrect password attempts is 3, and account lockout duration is 5 minutes.",
    "CLIs": [
        "local-aaa-user wrong-password retry-interval <retry-interval> retry-time <retry-time> block-time <block-time>",
        "undo local-aaa-user wrong-password"
    ],
    "ParentView": [
        "AAA view"
    ],
    "ParaDef": [
        {
            "Parameters": "retry-interval <retry-interval>",
            "Info": "Specifies the retry interval of a local account.\nThe value is an integer ranging from 5 to 65535."
        },
        {
            "Parameters": "retry-time <retry-time>",
            "Info": "Specifies the consecutive incorrect password attempts.\nThe value is an integer ranging from 3 to 65535."
        },
        {
            "Parameters": "block-time <block-time>",
            "Info": "Specifies the local account lockout duration. In actual applications, there is one minute difference in the lockout duration, and the actual lockout duration is less than the configured one.\nThe value is an integer ranging from 5 to 65535."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] aaa",
            "[~HUAWEI-aaa] local-aaa-user wrong-password retry-interval 5 retry-time 3 block-time 5"
        ]
    ],
    "ExtraInfo": "This command applies to the following scenarios:"
}