{
    "PageTitle": "ssm-mapping",
    "FuncDef": "The ssm-mapping command configures static source-specific multicast (SSM) mappings.\nThe undo ssm-mapping command deletes static SSM mappings. The ssm-mapping command configures static source-specific multicast (SSM) mappings. The undo ssm-mapping command deletes static SSM mappings. By default, no SSM mappings are configured.",
    "CLIs": [
        "ssm-mapping <group-address> { <mask> | <mask-length> } <source-address>",
        "undo ssm-mapping { <group-address> { <mask> | <mask-length> } [ <source-address> ] | staticall }"
    ],
    "ParentView": [
        "IGMP view,VPN instance IGMP view"
    ],
    "ParaDef": [
        {
            "Parameters": "<group-address>",
            "Info": "Specifies a multicast group address.\nThe value ranges from 224.0.1.0 to 239.255.255.255, in dotted decimal notation."
        },
        {
            "Parameters": "<mask>",
            "Info": "Specifies the mask of a multicast group address.\nThe value ranges from 224.0.1.0 to 255.255.255.255, in dotted decimal notation."
        },
        {
            "Parameters": "<mask-length>",
            "Info": "Specifies the mask length of a multicast group address.\nThe value is an integer that ranges from 5 to 32."
        },
        {
            "Parameters": "<source-address>",
            "Info": "Specifies a multicast source address.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "static",
            "Info": "Deletes static SSM mappings.\n-"
        },
        {
            "Parameters": "all",
            "Info": "Deletes all SSM mappings.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] multicast routing-enable",
            "[*HUAWEI] igmp",
            "[*HUAWEI-igmp] ssm-mapping 225.5.5.5 24 10.1.6.1"
        ]
    ],
    "ExtraInfo": "The default SSM group address range is from 232.0.0.0 to 232.255.255.255. If a host supports only IGMPv1/v2 Report messages, the host cannot join groups in the SSM group address range. If you want an IGMPv1/v2 host to join groups in the SSM group address range, run the ssm-mapping command configure static SSM mappings.\nGroup addresses not in the SSM group address range apply to the ASM model.\nThe configured SSM source/group address mapping entries take effect only after the igmp ssm-mapping enable command is run on the interface."
}