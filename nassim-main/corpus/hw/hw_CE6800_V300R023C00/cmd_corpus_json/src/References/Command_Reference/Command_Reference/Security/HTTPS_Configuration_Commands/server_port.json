{
    "PageTitle": "server port",
    "FuncDef": "The server port command configures an HTTP service listening port.\nThe undo server port command restores the default HTTP service listening port. The server port command configures an HTTP service listening port. The undo server port command restores the default HTTP service listening port. By default, HTTP service listening uses port 80.",
    "CLIs": [
        "server port <port-number>",
        "undo server port"
    ],
    "ParentView": [
        "Service-RESTCONF view"
    ],
    "ParaDef": [
        {
            "Parameters": "<port-number>",
            "Info": "Specifies the number for an HTTP service listening port.\nThe value can be 80 or an integer ranging from 1025 to 65535."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] http",
            "[*HUAWEI-http] service restconf",
            "[*HUAWEI-http-service-restconf] server port 1028"
        ]
    ],
    "ExtraInfo": ""
}