{
    "PageTitle": "discriminator (BFD session view)",
    "FuncDef": "The discriminator command sets the local and remote discriminators of a static BFD session.\nThe undo discriminator command deletes the local and remote discriminators of a static BFD session. The discriminator command sets the local and remote discriminators of a static BFD session. The undo discriminator command deletes the local and remote discriminators of a static BFD session. By default, the discriminator of a static BFD session is not created.",
    "CLIs": [
        "discriminator local <discr-value>",
        "discriminator remote <discr-value>",
        "undo discriminator local [ <discr-value> ]",
        "undo discriminator remote [ <discr-value> ]"
    ],
    "ParentView": [
        "BFD session view"
    ],
    "ParaDef": [
        {
            "Parameters": "remote <discr-value>",
            "Info": "Specifies the remote discriminator of a BFD session.\nThe value is an integer that ranges from 1 to 4294967295."
        },
        {
            "Parameters": "local <discr-value>",
            "Info": "Specifies the local discriminator of a BFD session.\nThe value is an integer that ranges from 1 to 16384 ."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bfd",
            "[*HUAWEI-bfd] quit",
            "[*HUAWEI] bfd s1 bind peer-ip 10.10.20.2",
            "[*HUAWEI-bfd-session-s1] quit",
            "[*HUAWEI] bfd s1",
            "[*HUAWEI-bfd-session-s1] discriminator local 80",
            "[*HUAWEI-bfd-session-s1] discriminator remote 800"
        ]
    ],
    "ExtraInfo": "During the establishment of a static BFD session, you must run the discriminator command to set the local and remote discriminators of the static BFD session; otherwise, the static BFD session cannot be established."
}