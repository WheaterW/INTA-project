{
    "PageTitle": "dfs-group m-lag",
    "FuncDef": "The dfs-group m-lag command binds a user-side interface to a DFS group.\nThe undo dfs-group m-lag command unbinds a user-side interface from a DFS group. The dfs-group m-lag command binds a user-side interface to a DFS group. The undo dfs-group m-lag command unbinds a user-side interface from a DFS group. By default, no user-side interface is bound to a DFS group.",
    "CLIs": [
        "dfs-group <dfs-group-id> m-lag <m-lag-id> [ active-standby ]",
        "undo dfs-group [ <dfs-group-id> [ m-lag <m-lag-id> [ active-standby ] ] ]"
    ],
    "ParentView": [
        "Eth-Trunk interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "<dfs-group-id>",
            "Info": "Indicates the ID of a DFS group.\nThe value must be 1."
        },
        {
            "Parameters": "<m-lag-id>",
            "Info": "Specifies the ID of a user-side interface. The specification is the same as that of an Eth-Trunk.\nThe value is an integer ranging from 1 to 2048."
        },
        {
            "Parameters": "active-standby",
            "Info": "Indicates the active/standby mode.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface eth-trunk 2",
            "[*HUAWEI-Eth-Trunk2] dfs-group 1 m-lag 3"
        ]
    ],
    "ExtraInfo": "Ensure that the bound Eth-Trunk interface is connected to a device that needs to be dual-homed to the M-LAG, and the two M-LAG devices must be configured with the same m-lag-id.\nIf the active-standby parameter is not specified when this command is run, M-LAG member interfaces work in active/active mode.\nIf the active-standby parameter is specified when this command is run, M-LAG member interfaces work in active/standby mode.\nBefore configuring M-LAG member interfaces in active/standby mode, configure the packet type for active/standby interface election."
}