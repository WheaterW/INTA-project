{
    "PageTitle": "graceful-shutdown all-peer",
    "FuncDef": "The graceful-shutdown all-peer command enables the g-shut feature globally.\nThe undo graceful-shutdown all-peer command restores the default configuration. The graceful-shutdown all-peer command enables the g-shut feature globally. The undo graceful-shutdown all-peer command restores the default configuration. By default, the g-shut feature is not enabled globally.",
    "CLIs": [
        "graceful-shutdown all-peer",
        "undo graceful-shutdown all-peer"
    ],
    "ParentView": [
        "BGP view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] graceful-shutdown all-peer"
        ]
    ],
    "ExtraInfo": "To enable the g-shut feature for all peers, run this command."
}