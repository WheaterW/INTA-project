{
    "PageTitle": "arp detect interval (System view)",
    "FuncDef": "The arp detect interval command sets the aging probe interval of dynamic ARP entries.\nThe undo arp detect interval command restores the default setting. The arp detect interval command sets the aging probe interval of dynamic ARP entries. The undo arp detect interval command restores the default setting. By default, the aging probe interval of dynamic ARP entries is 5 seconds.",
    "CLIs": [
        "arp detect interval <interval-value>",
        "undo arp detect interval [ <interval-value> ]"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "interval <interval-value>",
            "Info": "Specifies the aging probe interval of dynamic ARP entries.\nThe value is an integer ranging from 1 to 5, in seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] arp detect interval 3"
        ]
    ],
    "ExtraInfo": "To set the aging probe interval of dynamic ARP entries, run the arp detect interval command. Before aging a dynamic ARP entry, the system initiates detection. If the system does not receive any response within the detection interval, the system deletes the entry."
}