{
    "PageTitle": "display bgp network",
    "FuncDef": "The display bgp network command displays the routes imported into the BGP routing table using the network command. The display bgp network command displays the routes imported into the BGP routing table using the network command.",
    "CLIs": [
        "display bgp network",
        "display bgp vpnv4 all network",
        "display bgp vpnv4 vpn-instance <vpn-instance-name> network",
        "display bgp instance <instance-name> vpnv4allnetwork",
        "display bgp instance <instance-name> vpnv4vpn-instance <vpn-instance-name> network"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "vpnv4",
            "Info": "Displays the VPNv4 routes that are advertised using the network command.\n-"
        },
        {
            "Parameters": "all",
            "Info": "Displays all the VPNv4 routes that are advertised using the network command.\n-"
        },
        {
            "Parameters": "vpn-instance <vpn-instance-name>",
            "Info": "Displays the routes of a specified VPN instance that are advertised using the network command.\nThe value is a string of 1 to 31 case-sensitive characters. It cannot contain spaces. The VPN instance name cannot be _public_. If the character string is quoted by double quotation marks, the character string can contain spaces."
        },
        {
            "Parameters": "instance <instance-name>",
            "Info": "Specifies a BGP instance.\nThe value is a string of 1 to 31 case-sensitive characters without spaces. When double quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display bgp network"
        ],
        [
            "<HUAWEI> display bgp vpnv4 all network"
        ]
    ],
    "ExtraInfo": ""
}