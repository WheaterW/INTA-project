{
    "PageTitle": "peer sa-policy export",
    "FuncDef": "The peer sa-policy export command configures a policy for filtering (S, G) information in source active (SA) messages to be forwarded to a specified Multicast Source Discovery Protocol (MSDP) peer.\nThe undo peer sa-policy export command restores the default configuration. The peer sa-policy export command configures a policy for filtering (S, G) information in source active (SA) messages to be forwarded to a specified Multicast Source Discovery Protocol (MSDP) peer. The undo peer sa-policy export command restores the default configuration. By default, (S, G) information in SA messages is not filtered, so that all (S, G) entry information can be forwarded to an MSDP peer.",
    "CLIs": [
        "peer <peer-address> sa-policyexport { <advanced-acl-number> | acl-name <acl-name> }",
        "peer <peer-address> sa-policyexport",
        "undo peer <peer-address> sa-policyexport"
    ],
    "ParentView": [
        "VPN instance MSDP view,MSDP view of a public network instance"
    ],
    "ParaDef": [
        {
            "Parameters": "<peer-address>",
            "Info": "Specifies the address of an MSDP peer.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "<advanced-acl-number>",
            "Info": "Specifies the number of an advanced ACL.\nThe value is an integer ranging from 3000 to 3999. If the ACL is not specified, all SA messages carrying (S, G) information are filtered."
        },
        {
            "Parameters": "acl-name <acl-name>",
            "Info": "Specifies the name of an advanced ACL.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported. The name must start with a letter or digit, and cannot contain only digits."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl number 3100",
            "[*HUAWEI-acl4-advance-3100] rule permit ip source 10.15.0.0 0.0.255.255 destination 225.1.0.0 0.0.255.255",
            "[*HUAWEI-acl4-advance-3100] quit",
            "[*HUAWEI] multicast routing-enable",
            "[*HUAWEI] msdp",
            "[*HUAWEI-msdp] peer 10.10.7.6 connect-interface Vlanif 1",
            "[*HUAWEI-msdp] peer 10.10.7.6 sa-policy export 3100"
        ]
    ],
    "ExtraInfo": "To filter (S, G) information in SA messages to be forwarded to a specified MSDP peer, run the peer sa-policy export command, implementing control over the receiving or sending of multicast source information.In addition, you can run the import-source command on an MSDP peer to specify (S, G) information that can be advertised by SA messages."
}