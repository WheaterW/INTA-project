{
    "PageTitle": "mkdir",
    "FuncDef": "The mkdir command creates a new directory on the FTP server. The mkdir command creates a new directory on the FTP server.",
    "CLIs": [
        "mkdir <remote-directory>"
    ],
    "ParentView": [
        "FTP client view"
    ],
    "ParaDef": [
        {
            "Parameters": "<remote-directory>",
            "Info": "Specifies the name of the destination directory.\nThe value is a string of 1 to 128 case-sensitive characters, spaces not supported."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> ftp 10.18.26.133",
            "[ftp] mkdir new_folder"
        ]
    ],
    "ExtraInfo": "By executing the mkdir command creates a subdirectory. The new remote directory name cannot have the same name as any other subdirectory or file in the specified directory."
}