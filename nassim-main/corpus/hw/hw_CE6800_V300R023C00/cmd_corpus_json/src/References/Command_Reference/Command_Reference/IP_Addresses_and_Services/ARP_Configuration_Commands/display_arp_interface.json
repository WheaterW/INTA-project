{
    "PageTitle": "display arp interface",
    "FuncDef": "The display arp interface command displays the ARP entries of a specified interface. The display arp interface command displays the ARP entries of a specified interface.",
    "CLIs": [
        "display arp interface { <interface-name> | <interface-type> <interface-number> }"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "interface <interface-type> <interface-number>",
            "Info": "Displays ARP entries learned by a specified interface.\n-"
        },
        {
            "Parameters": "interface <interface-name>",
            "Info": "Displays ARP entries learned by a specified interface.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display arp interface vbdif 100"
        ],
        [
            "<HUAWEI> display arp interface 100ge1/0/1"
        ]
    ],
    "ExtraInfo": "To check entries of all interface boards, run the display arp all command.\nTo check ARP entries learned by an interface or locate ARP faults on an interface, run the display arp interface command. The specified interface can be a Layer 3 interface or a Layer 2 interface added to a VLAN."
}