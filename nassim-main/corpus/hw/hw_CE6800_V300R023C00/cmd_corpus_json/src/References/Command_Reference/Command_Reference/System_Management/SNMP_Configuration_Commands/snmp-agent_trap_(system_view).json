{
    "PageTitle": "snmp-agent trap (system view)",
    "FuncDef": "The snmp-agent trap disable command disables all traps in the system.\nThe undo snmp-agent trap disable command restores the default status of all traps.\nThe snmp-agent trap enable command enables all traps in the system.\nThe undo snmp-agent trap enable command restores the default status of all traps. The snmp-agent trap disable command disables all traps in the system. The undo snmp-agent trap disable command restores the default status of all traps. The snmp-agent trap enable command enables all traps in the system. The undo snmp-agent trap enable command restores the default status of all traps. By default, the preceding command is not configured. Different trap functions of different modules have different default states. You can run the display snmp-agent trap all command to view the default states.",
    "CLIs": [
        "snmp-agent trap enable",
        "snmp-agent trap disable",
        "undo snmp-agent trap { enable | disable }"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "disable",
            "Info": "disable.\n-"
        },
        {
            "Parameters": "enable",
            "Info": "enable.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] snmp-agent trap disable"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] snmp-agent trap enable"
        ]
    ],
    "ExtraInfo": "The snmp-agent trap enable command must be used together with the snmp-agent target-host inform command or snmp-agent target-host command.\nTo enable a device to send trap messages, run at least the snmp-agent target-host inform command or snmp-agent target-host command on the device to specify the destination address of the trap messages.\nTo enable the trap function for all features, run the snmp-agent trap enable command. To enable the trap function for a specified feature, run the snmp-agent trap enable feature-name command."
}