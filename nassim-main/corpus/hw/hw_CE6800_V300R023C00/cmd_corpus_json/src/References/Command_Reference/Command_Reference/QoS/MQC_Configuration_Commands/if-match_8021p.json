{
    "PageTitle": "if-match 8021p",
    "FuncDef": "The if-match 8021p command configures a matching rule based on the 802.1p priority of VLAN packets in a traffic classifier.\nThe undo if-match 8021p command deletes a matching rule based on the 802.1p priority of VLAN packets in a traffic classifier. The if-match 8021p command configures a matching rule based on the 802.1p priority of VLAN packets in a traffic classifier. The undo if-match 8021p command deletes a matching rule based on the 802.1p priority of VLAN packets in a traffic classifier. By default, a matching rule based on the 802.1p priority of VLAN packets is not configured in a traffic classifier.",
    "CLIs": [
        "if-match 8021p <8021p-value> &<1-8>",
        "undo if-match 8021p",
        "undo if-match 8021p <8021p-value> &<1-8>"
    ],
    "ParentView": [
        "Traffic classifier view"
    ],
    "ParaDef": [
        {
            "Parameters": "<8021p-value>",
            "Info": "Specifies the 802.1p priority in VLAN packets.\nThe value is an integer that ranges from 0 to 7. A larger value indicates a higher priority."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] traffic classifier c1",
            "[*HUAWEI-classifier-c1] if-match 8021p 2"
        ]
    ],
    "ExtraInfo": "You can run the if-match 8021p command to classify traffic based on the 802.1p priority in VLAN packets so that the device processes packets matching the same traffic classifier in the same manner."
}