{
    "PageTitle": "dual-active detection delay",
    "FuncDef": "The dual-active detection delay command sets the dual-active detection delay upon failure of the peer-link.\nThe undo dual-active detection delay command restores the default dual-active detection delay upon failure of the peer-link. The dual-active detection delay command sets the dual-active detection delay upon failure of the peer-link. The undo dual-active detection delay command restores the default dual-active detection delay upon failure of the peer-link. By default, the dual-active detection delay upon failure of the peer-link is 3 seconds.",
    "CLIs": [
        "dual-active detection delay <delaytime>",
        "undo dual-active detection delay <delaytime>"
    ],
    "ParentView": [
        "DFS group view"
    ],
    "ParaDef": [
        {
            "Parameters": "<delaytime>",
            "Info": "Dual-active detection delay upon failure of the peer-link.\nThe value is an integer that ranges from 0 to 10000, in milliseconds. The value must be a multiple of 100. The default value is 3000."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] dfs-group 1",
            "[*HUAWEI-dfs-group-1] dual-active detection delay 100"
        ]
    ],
    "ExtraInfo": "If the peer-link fails but the DAD heartbeat status is normal, accelerated DAD is triggered immediately and three DAD packets are separately sent at intervals of 200 ms. As a result, exceptions occur in the following scenarios:"
}