{
    "PageTitle": "reset bgp",
    "FuncDef": "The reset bgp command resets specified BGP connections. The reset bgp command resets specified BGP connections.",
    "CLIs": [
        "reset bgp { <as-number> | <ipv4-address> | external | internal | group <group-name> | <ipv6-address> }",
        "reset bgp all",
        "reset bgp ipv4 all",
        "reset bgp vpn-instance <vpn-instance-name> ipv4-family { all | <as-number> | <ipv4-address> | external | internal | group <group-name> }",
        "reset bgp vpn-instance <vpn-instance-name> ipv4-family [ <ipv4-address> ] slow-peer",
        "reset bgp ipv4 [ <ipv4-address> | <ipv6-address> ] slow-peer",
        "reset bgp ipv4 [ <ipv4-address> ] slow-peer"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "<as-number>",
            "Info": "Specifies a 2-byte AS number (number<1-65535>) or a 4-byte AS number (number<1-65535>.number<0-65535>).\nFor an integral AS number, the value ranges from 1 to 4294967295.\nFor an AS number in dotted notation, the value is in the format of x.y, where x and y are integers ranging from 1 to 65535 and from 0 to 65535, respectively."
        },
        {
            "Parameters": "<ipv4-address>",
            "Info": "Resets the BGP connection with a specified peer.\nIt is in dotted decimal notation."
        },
        {
            "Parameters": "external",
            "Info": "Reset all EBGP connections.\n-"
        },
        {
            "Parameters": "internal",
            "Info": "Resets all IBGP connections.\n-"
        },
        {
            "Parameters": "group <group-name>",
            "Info": "Resets BGP connections with the specified peer group.\nThe value is a string of 1 to 47 case-sensitive characters, spaces not supported.  The character string can contain spaces if it is enclosed with double quotation marks (\")."
        },
        {
            "Parameters": "ipv4",
            "Info": "Resets BGP IPv4 connections.\n-"
        },
        {
            "Parameters": "all",
            "Info": "Reset all BGP connections.\n-"
        },
        {
            "Parameters": "<ipv6-address>",
            "Info": "Resets the TCP connection with a specified BGP4+ peer (all the routes learned by using the connection are deleted).\nThe value is in the format of X:X:X:X:X:X:X:X."
        },
        {
            "Parameters": "slow-peer",
            "Info": "Restores a slow peer connection to a normal peer connection.\n-"
        },
        {
            "Parameters": "vpn-instance <vpn-instance-name>",
            "Info": "Resets the connection of a specified VPN instance.\nThe value is a string of 1 to 31 case-sensitive characters. It cannot contain spaces. In addition, the VPN instance name must not be _public_. The character string can contain spaces if it is enclosed in double quotation marks (\"\")."
        },
        {
            "Parameters": "ipv4-family",
            "Info": "Indicates the IPv4 address family.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> reset bgp all"
        ]
    ],
    "ExtraInfo": "After BGP configurations change, you can run the reset bgp command to make the new configurations take effect immediately.\nIf a device does not support route-refresh, you can run the reset bgp command to make the new BGP routing policy take effect after the BGP routing policy changes.\nYou can run the reset bgp ipv4 all command to reset all BGP IPv4 connections of the public network.\nreset bgp as-numbercan reset only BGP connections on the public network. To reset BGP connections on a private network, specify a VPN instance."
}