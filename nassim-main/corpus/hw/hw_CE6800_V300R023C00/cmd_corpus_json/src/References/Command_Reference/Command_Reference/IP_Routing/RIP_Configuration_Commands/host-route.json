{
    "PageTitle": "host-route",
    "FuncDef": "The host-route command allows host routes to be added to the routing table.\nThe undo host-route command prevents host routes from being added to the routing table. The host-route command allows host routes to be added to the routing table. The undo host-route command prevents host routes from being added to the routing table. By default, host routes can be added to the routing table.",
    "CLIs": [
        "host-route",
        "undo host-route"
    ],
    "ParentView": [
        "RIP view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] rip 1",
            "[*HUAWEI-rip-1] host-route"
        ]
    ],
    "ExtraInfo": "By default, a device selects a mask based on the following rules after receiving RIP-1 routes:"
}