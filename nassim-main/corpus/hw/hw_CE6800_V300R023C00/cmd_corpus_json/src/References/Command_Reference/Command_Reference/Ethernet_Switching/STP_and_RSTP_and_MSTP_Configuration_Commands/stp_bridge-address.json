{
    "PageTitle": "stp bridge-address",
    "FuncDef": "The stp bridge-address command configures a bridge MAC address used by the device to calculate the spanning tree.\nThe undo stp bridge-address command restores the default configuration. The stp bridge-address command configures a bridge MAC address used by the device to calculate the spanning tree. The undo stp bridge-address command restores the default configuration. By default, a device uses its MAC address as the bridge MAC address to calculate the spanning tree.",
    "CLIs": [
        "stp bridge-address <mac-address>",
        "undo stp bridge-address"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<mac-address>",
            "Info": "Specifies a bridge MAC address used by the device to calculate the spanning tree.\nThe value is in the H-H-H format. H is a 4-bit hexadecimal number, such as 00e0 or fc01. If an H contains fewer than 4 digits, the left-most digits are padded with zeros. For example, e0 is displayed as 00e0. The MAC address cannot be set to FFFF-FFFF-FFFF."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] stp bridge-address 00e0-fc12-3456"
        ]
    ],
    "ExtraInfo": "In some special applications, two devices must simulate one root bridge. Specifically, the bridge IDs of BPDUs sent by both devices must be the same. However, because each device has a unique MAC address, the bridge IDs of devices are different. To configure the same bridge MAC address for the two devices, run the stp bridge-address command."
}