{
    "PageTitle": "zip",
    "FuncDef": "The zip command compresses files or directories. The zip command compresses files or directories.",
    "CLIs": [
        "zip <source-filename> <destination-filename> [ password <password> ]"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "<source-filename>",
            "Info": "Specifies the name of the source file.\nThe value is a string of case-sensitive characters in the format of [ <drive> ][ <path> ][ <file-name> ]. An absolute <path> name is a string of 1 to 255 characters. A relative <path> name is a string of 1 to 128\ncharacters. Up to 8 levels of directories are supported.\nAn absolute <path> is in the format of <drive> [ <path> ][ <file-name> ], and a relative <path> is in the format of [ <path> ][ <file-name> ]. That is, a relative <path> is the root <path> of the current working <path>."
        },
        {
            "Parameters": "<destination-filename>",
            "Info": "Specifies the name of the destination file.\nThe value is a string of case-sensitive characters in the format of [ <drive> ][ <path> ][ <file-name> ]. An absolute <path> name is a string of 1 to 255 characters. A relative <path> name is a string of 1 to 128\ncharacters. Up to 8 levels of directories are supported.\nAn absolute <path> is in the format of <drive> [ <path> ][ <file-name> ], and a relative <path> is in the format of [ <path> ][ <file-name> ]. That is, a relative <path> is the root <path> of the current working <path>."
        },
        {
            "Parameters": "password <password>",
            "Info": "Specifies the password.\nThe value is a string of 8 to 20 case-sensitive characters, spaces not supported."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> zip dir1 dir1.zip",
            "Info: Compress flash:/dir1 to flash:/dir1.zip? [Y/N]:Y"
        ],
        [
            "<HUAWEI> zip file1.txt file1.zip",
            "Info: Compress flash:/file1.txt to flash:/file1.zip? [Y/N]:Y"
        ]
    ],
    "ExtraInfo": "If the file is too large, you can run the zip command to compress the file to facilitate storage.To view detailed information about a compressed file, run the unzip command to decompress the file.The system supports compression/decompression on the local board and inter-board compression/decompression.If you want to compress multiple files, move or copy them to the same directory and run the zip command to compress the directory.The unzip command cannot decompress a package that contains multiple files."
}