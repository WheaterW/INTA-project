{
    "PageTitle": "ssh server timeout",
    "FuncDef": "The ssh server timeout command sets the authentication timeout period of the SSH server.\nThe undo ssh server timeout command restores the default authentication timeout period of the SSH server. The ssh server timeout command sets the authentication timeout period of the SSH server. The undo ssh server timeout command restores the default authentication timeout period of the SSH server. By default, the SSH authentication timeout period is 60 seconds.",
    "CLIs": [
        "ssh server timeout <seconds>",
        "undo ssh server timeout"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<seconds>",
            "Info": "Specifies the login timeout period of the SSH connection.\nIt is an integer data type. The value ranges from 1 to 120 seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ssh server timeout 90"
        ]
    ],
    "ExtraInfo": "The configuration takes effect during the next login.\nThis command takes effect for both IPv4 and IPv6 connections."
}