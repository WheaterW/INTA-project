{
    "PageTitle": "statistics enable (VLANIF interface view)",
    "FuncDef": "The statistics enable command enables the traffic statistics function on a VLANIF interface.\nThe undo statistics enable command disables the traffic statistics function on a VLANIF interface. The statistics enable command enables the traffic statistics function on a VLANIF interface. The undo statistics enable command disables the traffic statistics function on a VLANIF interface. By default, the traffic statistics function is disabled on a VLANIF interface.",
    "CLIs": [
        "statistics { ipv4 | ipv6 } enable [ inbound | outbound ]",
        "undo statistics { ipv4 | ipv6 } enable [ inbound | outbound ]"
    ],
    "ParentView": [
        "VLANIF interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "ipv4",
            "Info": "Enables traffic statistics collection on IPv4 packets.\n-"
        },
        {
            "Parameters": "ipv6",
            "Info": "Enables traffic statistics collection on IPv6 packets.\n-"
        },
        {
            "Parameters": "inbound",
            "Info": "Enables traffic statistics collection in the inbound direction of an interface.\n-"
        },
        {
            "Parameters": "outbound",
            "Info": "Enables traffic statistics collection in the outbound direction of an interface.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] vlan 100",
            "[~HUAWEI] interface vlanif 100",
            "[*HUAWEI-Vlanif100] statistics ipv4 enable",
            "[*HUAWEI-Vlanif100] statistics ipv6 enable"
        ]
    ],
    "ExtraInfo": "To check the network status or locate network faults, you can use the statistics enable command to enable the traffic statistics function on VLANIF interfaces. The device then collects traffic statistics on the VLANIF interfaces."
}