{
    "PageTitle": "dns resolve",
    "FuncDef": "The dns resolve command enables dynamic DNS resolution.\nThe undo dns resolve command disables dynamic DNS resolution. The dns resolve command enables dynamic DNS resolution. The undo dns resolve command disables dynamic DNS resolution. By default, dynamic DNS resolution is disabled.",
    "CLIs": [
        "dns resolve",
        "undo dns resolve"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] dns resolve"
        ]
    ],
    "ExtraInfo": "To obtain IP addresses mapping domain names from the DNS server, run the dns resolve command to enable dynamic DNS resolution on the device."
}