{
    "PageTitle": "icmp broadcast-address echo disable",
    "FuncDef": "The icmp broadcast-address echo disable command disables the system from responding to ICMP echo messages whose destination addresses are broadcast addresses.\nThe undo icmp broadcast-address echo disable command enables the system to respond to ICMP echo messages whose destination addresses are broadcast addresses. The icmp broadcast-address echo disable command disables the system from responding to ICMP echo messages whose destination addresses are broadcast addresses. The undo icmp broadcast-address echo disable command enables the system to respond to ICMP echo messages whose destination addresses are broadcast addresses. By default, the system responds to all ICMP request messages.",
    "CLIs": [
        "icmp broadcast-address echo disable",
        "undo icmp broadcast-address echo disable"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] icmp broadcast-address echo disable"
        ]
    ],
    "ExtraInfo": "Network attackers may launch attacks with ICMP messages whose destination addresses are broadcast addresses, increasing the traffic burden and deteriorating device performance. To improve device performance and enhance network security, run the icmp broadcast-address echo disable command to disable the system from responding to ICMP echo messages whose destination addresses are broadcast addresses. This configuration helps defend against attacks by such ICMP messages."
}