{
    "PageTitle": "apply gateway-ip",
    "FuncDef": "The apply gateway-ip command sets a gateway IP address for routes.\nThe undo apply gateway-ip command cancels the setting of the gateway IP address for routes. The apply gateway-ip command sets a gateway IP address for routes. The undo apply gateway-ip command cancels the setting of the gateway IP address for routes. By default, no gateway IP address is set for routes.",
    "CLIs": [
        "apply gateway-ip { origin-nexthop | <address> }",
        "undo apply gateway-ip [ <address> | origin-nexthop ]"
    ],
    "ParentView": [
        "Route-policy view"
    ],
    "ParaDef": [
        {
            "Parameters": "origin-nexthop",
            "Info": "Sets the next-hop IP address of a route as its gateway IP address.\n-"
        },
        {
            "Parameters": "<address>",
            "Info": "Sets a gateway IPv4 address for a route.\nThe value is in dotted decimal notation."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] route-policy policy permit node 10",
            "[*HUAWEI-route-policy] apply gateway-ip origin-nexthop"
        ]
    ],
    "ExtraInfo": "When configuring an L3VPN instance on a Layer 2 or Layer 3 gateway to advertise static VPN routes that can reach a VNF to an EVPN, you can create a route-policy to match such routes in the L3VPN instance and use the apply gateway-ip origin-nexthop command to set the original next hop addresses of these routes as the gateway addresses."
}