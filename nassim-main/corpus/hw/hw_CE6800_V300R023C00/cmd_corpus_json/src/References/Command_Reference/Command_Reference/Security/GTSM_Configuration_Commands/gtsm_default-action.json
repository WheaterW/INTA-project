{
    "PageTitle": "gtsm default-action",
    "FuncDef": "Using the gtsm default-action command, you can set the default action to be taken on the packets that do not match the GTSM policies. Using the gtsm default-action command, you can set the default action to be taken on the packets that do not match the GTSM policies. By default, the packets that do not match the GTSM policies can pass the filtering.",
    "CLIs": [
        "gtsm default-action { drop | pass }"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "drop",
            "Info": "Indicates that the packets that do not match the GTSM policies cannot pass the filtering and are dropped.\n-"
        },
        {
            "Parameters": "pass",
            "Info": "Indicates that the packets that do not match the GTSM policies can pass the filtering.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] gtsm default-action drop"
        ]
    ],
    "ExtraInfo": "The GTSM checks the validity of the TTL value of the IP packets that match the GTSM policies. For the packets that do not match the policies, GTSM takes the default action, that is, sending the packets to the main control board or dropping them."
}