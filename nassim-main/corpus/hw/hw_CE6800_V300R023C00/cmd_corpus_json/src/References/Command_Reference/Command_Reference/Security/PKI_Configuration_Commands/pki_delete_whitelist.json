{
    "PageTitle": "pki delete whitelist",
    "FuncDef": "The pki delete whitelist command deletes certificate whitelist files from the memory. The pki delete whitelist command deletes certificate whitelist files from the memory.",
    "CLIs": [
        "pki delete whitelist filename <file-name>"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "filename <file-name>",
            "Info": "Specifies the name of a certificate whitelist file.\nThe value must be an existing certificate whitelist file name and cannot contain tildes, backslashes (\\), spaces, double quotation marks (\"), single quotation marks ('), asterisks (*), colons (:), less-than signs (<), question marks (?), slashes (/), or vertical bars (|)."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] pki delete whitelist filename pki_bls.xml"
        ]
    ],
    "ExtraInfo": "If a certificate whitelist file is no longer required, run this command to delete it from the memory."
}