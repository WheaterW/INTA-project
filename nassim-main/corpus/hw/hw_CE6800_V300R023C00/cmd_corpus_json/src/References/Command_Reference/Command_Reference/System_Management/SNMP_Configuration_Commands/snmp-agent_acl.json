{
    "PageTitle": "snmp-agent acl",
    "FuncDef": "The snmp-agent acl command configures an ACL at SNMP protocol level.\nThe undo snmp-agent acl command deletes the ACL configuration at SNMP protocol level. The snmp-agent acl command configures an ACL at SNMP protocol level. The undo snmp-agent acl command deletes the ACL configuration at SNMP protocol level. By default, no ACL is configured at SNMP protocol level.",
    "CLIs": [
        "snmp-agent acl { <acl-number> | <aclName> }",
        "undo snmp-agent acl"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<acl-number>",
            "Info": "Specifies the number of an ACL.\nIt is an integer ranging from 2000 to 3999."
        },
        {
            "Parameters": "<aclName>",
            "Info": "Specifies an ACL name.\nIf no matching rule is configured for the referenced ACL, the matching rule is permit by default.\nThe value is a string of 1 to 32  case-sensitive characters, spaces not supported."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl 2000",
            "[*HUAWEI-acl4-basic-2000] quit",
            "[*HUAWEI] acl ipv6 2000",
            "[*HUAWEI-acl6-basic-2000] quit",
            "[*HUAWEI] snmp-agent acl 2000"
        ]
    ],
    "ExtraInfo": "To improve security, run this command to allow only SNMP users included in ACL to access a specific device.\nIf you need to specify an ACL when configuring an SNMP community name, SNMP group, or SNMP user, run this command first."
}