{
    "PageTitle": "tftp server ipv6 acl",
    "FuncDef": "The tftp server ipv6 acl command enables the IPv6-based ACL configuration.\nThe undo tftp server ipv6 acl command cancels the IPv6-based access control of the TFTP server that the local device can access. The tftp server ipv6 acl command enables the IPv6-based ACL configuration. The undo tftp server ipv6 acl command cancels the IPv6-based access control of the TFTP server that the local device can access. By default, no ACL is configured for the TFTP client.",
    "CLIs": [
        "tftp server ipv6 acl { <acl6-number> | <acl6-name> }",
        "undo tftp server ipv6 acl"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<acl6-number>",
            "Info": "Specifies the IPv6 ACL number.\nACL number is an integer data type. The basic acl number value ranges from 2000 to 2999."
        },
        {
            "Parameters": "<acl6-name>",
            "Info": "Specifies the ACL6 name.\nThe value is a string of 1 to 32 case-sensitive characters without spaces. The value must start with a letter (a to z or A to Z, case sensitive)."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl ipv6 name test",
            "[*HUAWEI-acl6-advance-test] quit",
            "[*HUAWEI] tftp server ipv6 acl test"
        ]
    ],
    "ExtraInfo": "If a device serves as a TFTP client, you can configure the ACL on the device to control the login of the local device to the TFTP server through TFTP."
}