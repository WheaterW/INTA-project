{
    "PageTitle": "dldp interval",
    "FuncDef": "The dldp interval command sets the interval at which Advertisement packets are sent.\nThe undo dldp interval command restores the default setting. The dldp interval command sets the interval at which Advertisement packets are sent. The undo dldp interval command restores the default setting. The default interval at which Advertisement packets are sent is 5 seconds.",
    "CLIs": [
        "dldp interval <interval>",
        "undo dldp interval"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "<interval>",
            "Info": "Specifies the interval at which Advertisement packets are sent.\nThe value is an integer ranging from 1 to 100, in seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] dldp interval 20"
        ]
    ],
    "ExtraInfo": "Running the dldp interval command sets the interval at which Advertisement packets are sent. Setting the interval allows DLDP to detect the existence of unidirectional links promptly in different network environments.\nGenerally, the interval at which Advertisement packets are sent is less than one third of the STP convergence time."
}