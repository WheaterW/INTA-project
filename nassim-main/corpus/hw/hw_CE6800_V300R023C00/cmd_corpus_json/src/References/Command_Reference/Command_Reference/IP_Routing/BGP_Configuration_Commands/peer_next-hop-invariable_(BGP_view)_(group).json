{
    "PageTitle": "peer next-hop-invariable (BGP view) (group)",
    "FuncDef": "The peer next-hop-invariable command configures the device not to change the next hop of a route when advertising the route to a peer.\nThe undo peer next-hop-invariable command restores the default configuration. The peer next-hop-invariable command configures the device not to change the next hop of a route when advertising the route to a peer. The undo peer next-hop-invariable command restores the default configuration. By default:",
    "CLIs": [
        "peer <group-name> next-hop-invariable [ include-static-route | include-unicast-route ]",
        "undo peer <group-name> next-hop-invariable"
    ],
    "ParentView": [
        "BGP-IPv4 unicast address family view,BGP view"
    ],
    "ParaDef": [
        {
            "Parameters": "<group-name>",
            "Info": "Specifies the name of a peer group.\nThe value is a string of 1 to 47 case-sensitive characters, spaces not supported.  The character string can contain spaces if it is enclosed with double quotation marks (\")."
        },
        {
            "Parameters": "include-static-route",
            "Info": "Configures the device to retain the original Next_Hop of static routes when advertising the routes to the peer.\n-"
        },
        {
            "Parameters": "include-unicast-route",
            "Info": "Configures the device to retain the original next hop of the unicast routes learned from peers when advertising the routes.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bgp 100",
            "[*HUAWEI-bgp] group test external",
            "[*HUAWEI-bgp] peer test as-number 200",
            "[*HUAWEI-bgp] peer test next-hop-invariable include-static-route"
        ]
    ],
    "ExtraInfo": "The peer next-hop-invariable command has the following functions:"
}