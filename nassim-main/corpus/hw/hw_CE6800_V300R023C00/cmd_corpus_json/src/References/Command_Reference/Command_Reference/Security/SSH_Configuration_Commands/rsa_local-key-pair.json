{
    "PageTitle": "rsa local-key-pair",
    "FuncDef": "The rsa local-key-pair create command generates local RSA host and server key pairs.\nThe rsa local-key-pair destroy command removes all local RSA keys including the host key pair and the server key pair. The rsa local-key-pair create command generates local RSA host and server key pairs. The rsa local-key-pair destroy command removes all local RSA keys including the host key pair and the server key pair. By default, no local RSA host or server key pairs are generated.",
    "CLIs": [
        "rsa local-key-pair create",
        "rsa local-key-pair destroy"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] rsa local-key-pair destroy",
            "Confirm to destroy these keys? Please select [Y/N]: Y"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] rsa local-key-pair create",
            "Please input the modulus [default = 3072]:3072"
        ]
    ],
    "ExtraInfo": "When you run this command, if the RSA key already exists, the system prompts you to confirm whether to replace the original key. The generated key pair is named in the format of Host_Server and Host. This command is not saved in the configuration file.\nAfter running the destroy key-pair command, you need to confirm whether to remove all local RSA keys. This command is a one-time operation command and therefore is not saved in the configuration file."
}