{
    "PageTitle": "lsa-arrival-interval suppress-flapping",
    "FuncDef": "The lsa-arrival-interval suppress-flapping command configures a suppression period in case received OSPF LSAs flap.\nThe undo lsa-arrival-interval suppress-flapping command restores the default suppression period. The lsa-arrival-interval suppress-flapping command configures a suppression period in case received OSPF LSAs flap. The undo lsa-arrival-interval suppress-flapping command restores the default suppression period. By default, the suppression period is 10s, and the suppression threshold is 30.",
    "CLIs": [
        "lsa-arrival-interval suppress-flapping <suppress-interval> [ threshold <threshold> ]",
        "undo lsa-arrival-interval suppress-flapping"
    ],
    "ParentView": [
        "OSPF view"
    ],
    "ParaDef": [
        {
            "Parameters": "<suppress-interval>",
            "Info": "Specifies the suppression period that takes effect when received OSPF LSAs flap.\nThe value is an integer ranging from 0 to 600, in seconds. The default value is 10s."
        },
        {
            "Parameters": "threshold <threshold>",
            "Info": "Specifies the suppression threshold that takes effect when received OSPF LSAs flap.\nThe value is an integer ranging from 3 to 100. The default value is 30."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ospf 1",
            "[*HUAWEI-ospf-1] lsa-arrival-interval suppress-flapping 200"
        ]
    ],
    "ExtraInfo": ""
}