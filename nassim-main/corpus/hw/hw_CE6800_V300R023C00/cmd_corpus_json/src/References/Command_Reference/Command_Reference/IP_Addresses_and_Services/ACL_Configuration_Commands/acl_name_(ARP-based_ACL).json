{
    "PageTitle": "acl name (ARP-based ACL)",
    "FuncDef": "The acl name command creates an ARP-based ACL and displays the ACL view. If the ARP-based ACL to be created already exists, running this command directly displays the ACL view. The acl name command creates an ARP-based ACL and displays the ACL view. If the ARP-based ACL to be created already exists, running this command directly displays the ACL view. By default, no ARP-based ACL is created.",
    "CLIs": [
        "acl name <arp-acl-name> arp",
        "acl name <arp-acl-name> [ number ] <arp-acl-number>"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "arp",
            "Info": "Creates an ARP-based ACL with a keyword.\n-"
        },
        {
            "Parameters": "name <arp-acl-name>",
            "Info": "Creates an ARP-based ACL with a name.\nThe value is a string of 1 to 32 case-sensitive characters, spaces not supported. The name must start with a letter or digit, and cannot contain only digits."
        },
        {
            "Parameters": "number <arp-acl-number>",
            "Info": "Creates an ARP-based ACL with a number.\nThe value is an integer ranging from 23000 to 23999."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl name arp-acl arp"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl name arp-acl number 23999"
        ]
    ],
    "ExtraInfo": "An ARP-based ACL defines rules for filtering packets based on the source or destination IP or MAC addresses of ARP packets. To create an ARP-based ACL, run the acl command."
}