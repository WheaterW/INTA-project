{
    "PageTitle": "default-ip-address",
    "FuncDef": "The default-ip-address command configures the default multicast address for a BFD session.\nThe undo default-ip-address command restores the default multicast address for a BFD session. The default-ip-address command configures the default multicast address for a BFD session. The undo default-ip-address command restores the default multicast address for a BFD session. By default, the default multicast address for a BFD session is 224.0.0.184.",
    "CLIs": [
        "default-ip-address <default-ip-value>",
        "undo default-ip-address [ <default-ip-value> ]"
    ],
    "ParentView": [
        "BFD view"
    ],
    "ParaDef": [
        {
            "Parameters": "<default-ip-value>",
            "Info": "Specifies the multicast address for a BFD session.\nThe value ranges from 224.0.0.107 to 224.0.0.250, in dotted decimal notation."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] bfd",
            "[*HUAWEI-bfd] default-ip-address 224.0.0.150"
        ]
    ],
    "ExtraInfo": "If the physical status of a link is to be monitored and the peer IP address is unavailable (for example, when no peer IP address exists on an Eth-Trunk member link), you can bind a BFD session to a multicast address and enable BFD control packets to be sent to the multicast address before BFD monitors the physical status.\nYou must change the default multicast address in either of the following situations:"
}