{
    "PageTitle": "display ip interface multi-address",
    "FuncDef": "The display ip interface multi-address command displays all interfaces configured with multiple IP addresses. The display ip interface multi-address command displays all interfaces configured with multiple IP addresses.",
    "CLIs": [
        "display ip interface multi-address"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [],
    "Examples": [
        [
            "<HUAWEI> display ip interface multi-address"
        ]
    ],
    "ExtraInfo": "If an interface on a device is configured with multiple IP addresses, the device cannot be configured to differentiate primary IP addresses from secondary IP addresses. To check whether an interface configured with multiple IP addresses exists on a device, run the display ip interface multi-address command."
}