{
    "PageTitle": "ntp discard",
    "FuncDef": "The ntp discard command configures the minimum inter-packet interval and average minimum inter-packet interval.\nThe undo ntp discard command deletes the minimum inter-packet interval and average minimum inter-packet interval. The ntp discard command configures the minimum inter-packet interval and average minimum inter-packet interval. The undo ntp discard command deletes the minimum inter-packet interval and average minimum inter-packet interval. The default minimum inter-packet interval is 1, and the default average minimum packet interval is 5.",
    "CLIs": [
        "ntp discard { min-interval <min-interval-val> | avg-interval <avg-interval-val> }",
        "undo ntp discard"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "min-interval <min-interval-val>",
            "Info": "Specifies the minimum inter-packet spacing.\nThe value ranges from 1 to 8, in seconds. The actual minimum interval for sending packets is the nth power of 2, in seconds."
        },
        {
            "Parameters": "avg-interval <avg-interval-val>",
            "Info": "Specifies the average inter-packet spacing.\nThe value ranges from 1 to 8, in seconds. The actual average interval for sending packets is the nth power of 2, in seconds."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ntp discard min-interval 4 avg-interval 4"
        ]
    ],
    "ExtraInfo": "To set the global minimum inter-packet interval and average minimum inter-packet interval, run the ntp discard command, which is used to generate the RATE kiss code."
}