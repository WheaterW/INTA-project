Verifying the Configuration
===========================

After basic NAT64 functions are configured, you can verify the basic NAT64 configuration.

#### Procedure

* Run the [**display nat64 instance**](cmdqueryname=display+nat64+instance) command to check the NAT64 instance configuration.